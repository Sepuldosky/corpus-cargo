CARGO — ROADMAP
Qué sigue y en qué orden. Dónde estamos dentro de esto → cargo_estado.md.
=====================================================
Documento vivo. El detalle de diseño de cada punto vive en
Cargo_Architecture.md / Workbench_Arquitectura.md / Cargo_ItemImages_Arquitectura.md
/ Cargo_Trade_Arquitectura.md; acá solo el orden.

NOTA DE LECTURA (cita FLU-22 y el voto del autor del 2026-07-19): este doc es
INTENCIÓN PURA, no autoridad — nivel 6 de la jerarquía de §7.1 del flujo. Los IDs
que aparecen acá son CITAS a normas cuya sede está en un doc de diseño; este roadmap
no acuña ninguna y no puede ser sede de nada. Un "limpio" del gate de coherencia
sobre este archivo se reporta NO-AUDITABLE POR DISEÑO.
Lo destapó el COMPLETO del 2026-07-19: CRG-45 tenía su sede declarada acá y el
archivo ni siquiera contenía la etiqueta. Se mudó a Cargo_Architecture.md §13.1.
=====================================================


AHORA (cierre del Block 1)
---------------------------
1. Block 1 verificado en juego (2026-07-10). El batch de fixes de esa pasada
   + la captura de armas quedaron re-verificados en juego: CHANGELOG #2
   [APLICADO 2026-07-11].  [HECHO 2026-07-11]
2. Primer commit del repo cuando el autor lo pida.  [HECHO 2026-07-11]


SIGUIENTES BLOQUES
-------------------
3. UI FULLSCREEN ESTILO STALKER  [DISEÑO CERRADO 2026-07-11]
   La FUNCIONALIDAD y la disposición quedan; cambia la FORMA: inventario y
   pantalla de traslado/trueque a pantalla completa estilo STALKER. Diseño en
   tres documentos (sesión Opus + mockup HTML, cargo_fullscreen_ui_mock_v1.html):
     - FORMA del inventario  -> §15 nueva de Cargo_Architecture.md (layout 3
       columnas / 3 estados, orden STALKER del equipamiento, círculos de
       herramienta sandbox #21, cinturón de munición [solo forma de #19],
       botón de dinero). Enmienda §7: grid uniforme -> gradas por footprint.
     - COMERCIO  -> Cargo_Trade_Arquitectura.md (doc propio: primitivo
       inventario-en-entidad, basket con confirm, precio value×condición×spread
       configurable por-NPC/por-jugador, jugador-trader con doble confirm,
       entidad de dinero + traspaso P2P, NPC trader de ejemplo).
       [EN CURSO — corte de 3 slices validado con el autor 2026-07-13:
        (1) comercio con NPC: precio + spread + trader + estado Trade + basket
            atómico  -> CHANGELOG #20 [APLICADO 2026-07-14]. La pasada en juego
            YA CORRIÓ: derogó el peso como gate de la compra y rehizo el basket
            (entries #21 y #22, ambas APLICADAS y confirmadas). De la 2.ª pasada
            queda la #27 [PENDIENTE] (Sidorovich, strips parejos, N armas iguales).
        (2) dinero como entidad (botar / línea de solo-dinero en el basket).
        (3) jugador-trader con doble confirm (incluye el traspaso P2P).
        Estado por slice en Cargo_Trade_Arquitectura.md §12.bis. El primitivo
        inventario-en-entidad de §2 NO se construyó de nuevo: es
        Containers.Attach; el trader es ese contenedor + capa de precio.]
   PREREQUISITO DURO (ya satisfecho): #5 (imágenes) — las gradas no valían con
   fallback de letra. Bajó primero (CHANGELOG #5) y la implementación VGUI del
   inventario fullscreen corrió después, en su propio bloque (CHANGELOG #8).

4. MANOS DEFAULT (estado desarmado).  [APLICADO 2026-07-12, CHANGELOG #9 —
   confirmado in-game; solo queda remitir el fix de lighting a Twilight]
   Con la captura de armas activa
   el jugador spawnea desarmado; hoy queda el viewmodel vacío del engine.
   Decisión del autor (2026-07-11): reciclar "Apex Legends: Holster/Melee
   SWEP" (Twilight Sparkle + Buu342, Workshop 2792160770) como manos
   default de Corpus, con rename + créditos completos + política de retirar
   assets si el autor original lo pide (riesgo de licencia silenciosa
   asumido; ARC9 MW2019 de la misma autora NO se toca). Al reciclar:
   arreglar el bug de brazos oscuros (repro: se oscurece mirando al
   horizonte, recupera mirando arriba/abajo o flotando -> apunta al
   lighting origin del viewmodel muestreando el suelo), remitir el fix a
   Twilight, y meter su clase en CARGO.Capture.Ignore. Ver
   ../../dev/mods_workshop_mapa.md §2.
   -> Hecho como SWEP corpus_cargo_hands ("Hands"), clase (+apexswep) en
   Capture.Ignore, elección Hands/nada en el tab Q (cargo_holster_hands).
   Fix de brazos oscuros: 1.er intento (SetLightingOriginEntity -> jugador)
   NO sirvió; 2.º intento (SuppressEngineLighting + caja de luz propia
   muestreada en EyePos con piso, en PreDrawViewModel/PreDrawPlayerHands)
   CONFIRMADO in-game (2026-07-12, aplicó en vivo sin reiniciar). PENDIENTE:
   remitir el fix a Twilight (acción del autor).

5. SISTEMA DE IMÁGENES DE ÍTEMS
   [CERRADO — CHANGELOG #5, APLICADO 2026-07-11 (confirmado in-game el mismo
    día): implementado en corpus_cargo_icons.lua (server: overrides de cámara
    y footprint; client: pipeline modelo -> RT -> PNG con caché local) +
    corpus_cargo_iconeditor.lua (cargo_icon_edit + regen-all). El GATE DE
    TRANSPARENCIA lo ganó el PLAN A (alpha real: PNG transparentes limpios);
    el Plan B (fondo horneado) queda cableado como fallback en la convar
    cargo_icon_bake_bg (0 = A, default). Deuda viva: la fuente ARC9 es 256²
    a 1080p (aceptada en la 2.ª pasada, 2026-07-12).]
   -> Cargo_ItemImages_Arquitectura.md (doc propio). Render del modelo a RT
   (estilo spawnicon/Edit Icon), caché en data/corpus/cargo/icons/, encuadre
   con override en data para defs autogenerados, footprint con auto por OBB
   cuantizado + techo por categoría, editor cargo_icon_edit + regen-all.
   Era el PREREQUISITO DURO de la UI fullscreen (#3), que ya corre con
   gradas reales en vez del fallback de letra.

6. WORKBENCH (doc particular ya cerrado: Workbench_Arquitectura.md).
   Craft + Reparación por zona + Desarme con herencia de condición +
   economía de toolkits. Reutiliza el contrato de ítems, la clase única
   con blob y el sistema de placas de este Block 1. Upgrades queda
   pendiente dentro del propio doc (canal ARC9 identificado).


CRUCES CON OTROS MÓDULOS (cada uno espera el Block del dueño)
--------------------------------------------------------------
7. Caliber Block 3: mitigación real por Body (el ítem-armadura ya existe,
   pesa, se equipa y persiste condición por zona — falta el efecto),
   escudos de jugador vía sub-slot exo/escudo (punto de acoplamiento ya
   definido), barra de protección en el panel de estado.
8. Coagulant: drenaje de stamina por sobrepeso — el ÚNICO pendiente real del
   cruce (contrato congelado esperado: coagulant.OnEncumbrance(ply, fraction),
   ya invocado con lazy-check desde corpus_cargo_movement.lua; Coagulant hoy
   solo guarda la fracción, sin efecto).
   - YA CERRADO: los ítems médicos reales (4 defs categoría medical,
     verificadas en juego — rondas 1-4 de Coagulant).
   - EN CÓDIGO SIN VERIFICAR: la barra de sangre del panel (slice 4 de
     Coagulant, espera su ronda 7). Ojo: una barra "vital" separada NO
     existe — Coagulant registra UNA sola (Blood); la vida la pinta su
     silueta propia.
9. Craving: CERRADO, sin pendientes — 6 consumibles reales + barras de
   hambre/hidratación, verificado en juego (ver craving_estado.md).
10. Cortex: provider de facción/rango real (contrato congelado esperado:
    cortex.GetFactionInfo(ply) -> {faction, rank}) + disguise leyendo el
    hook Corpus_Cargo_BodyChanged / GetEquipped(ply, "body").
    + LOOT DE CADÁVERES: reusa el primitivo inventario-en-entidad de
    Cargo_Trade_Arquitectura.md §2/§9 (Cargo provee UI de loot; Cortex/Caliber
    dueños de loot table + muerte). Frontera declarada, se cierra en su bloque.


INTEGRACIONES / DEUDA PROPIA
-----------------------------
11. Mapear mods concretos de NVG/ópticas contra el sub-slot Head
    (dev/mods_workshop_mapa.md; COMPAT-RUNTIME).
12. Permisos de admin para comandos dev y acciones sensibles — espera la
    primitiva de Corpus (sesión de diseño aparte, TODO ya dejado en código).
    Alcanza también a los comandos dev nuevos: cargo_icon_edit / regen_all (#5)
    y el spawn del NPC trader de ejemplo (Trade §10).
    La norma es CRG-45 y su SEDE es Cargo_Architecture.md §13.1 — acá solo se
    ordena el trabajo. Ojo con el matiz que la sede enuncia, y que la tanda B5
    del 2026-07-26 cambió: de los net.Receive de SERVIDOR del módulo, casi todos
    no necesitan gate porque los protege CRG-6; el que lo espera y no lo tiene
    sigue siendo NET_ICON_OVERRIDE (contenido por CRG-46), y ahora hay UNO que
    sí trae gate porque invierte CRG-6 — NET_IMPORT (CRG-61, §12.1). Ese gate es
    LOCAL y provisional: es el primer consumidor que la primitiva va a tener el
    día que exista, y tiene su fila propia en §13. Los conteos exactos se leen
    de la sede, que se re-deriva del árbol (FLU-27); acá no se repiten.
13. Corpus.Data.Delete como primitiva del framework.
    [CERRADO por CHANGELOG #41 (2026-07-25): el file.Delete crudo de
     Instances.Delete YA NO EXISTE — el blob dejó de tener archivo propio
     (CRG-56), así que borrar es sacarlo de _live y no hay nada que
     desenlazar. Con eso desaparece la violación declarada de COR-3.]
    [COLETA CERRADA por CHANGELOG #43 (2026-07-25): la primitiva ya EXISTE.
     Corpus.Data expone List y Delete, más el scope de COR-19, y su primer
     consumidor real es cargo_dev_purge_legacy — el comando que lista y purga
     los inst_* legacy que un tercero pueda tener todavía en disco. Esta línea
     no deja nada abierto.]
14. addon.json + empaquetado Workshop (deuda compartida del ecosistema).
15. Loot on death — diseño posterior.
    [El "GC de instancias huérfanas" que esta línea traía SALE del pendiente:
     la clase dejó de existir con CHANGELOG #41 (CRG-56). Un blob sin dueño ya
     no puede quedar en disco porque no tiene archivo propio, y el render del
     archivo de dueño se reconstruye entero en cada guardado.]
    [La segunda arma de una clase ya poseída YA NO se absorbe — CHANGELOG #27,
     pedido del autor 2026-07-14: cada arma capturada es su propia instancia, y
     el dedup quedó acotado al give ANÓNIMO del engine (el loadout, que si no
     stockearía una copia por respawn). Una toma deliberada captura la N-ésima.]
    Enlaza con loot de cadáveres (Trade §9, cruce Cortex).
    PARCIAL (CHANGELOG #10, [APLICADO 2026-07-12], confirmado en juego): variante WIPE lista
    (cargo_lose_on_death borra todo + dinero al morir; cargo_persistence 0 =
    sesión en memoria, y desde #41 eso significa que NO se escribe nada — antes
    los blobs de instancia se colaban igual). Lo que sigue pendiente es la
    variante LOOT (todo cae recuperable) — decisión del autor fue wipe, no loot.


BLOQUE DE MANEJO / INTERACCIÓN DE ARMAS (hallazgos in-game 2026-07-11)
-------------------------------------------------------------------------------------
Hallazgos del autor probando en juego, tras la baja del mod "L4D Item Pickup"
(CHANGELOG #4). MATERIAL DE ENTRADA para la sesión de diseño del bloque de
mejoras de UI/manejo de armas. La FRONTERA de dueño (Cargo = pickup/drop/equip/
UI/quickslots; Caliber = semántica del arma: munición, cargador, condición→traba)
se resuelve AL diseñar el bloque. Varios tocan la UI fullscreen (#3).
Espejado como cross-ref en caliber_roadmap.txt §2[4].

Estado de diseño de este bloque tras la sesión 2026-07-11:
  - #21 (círculos de herramienta sandbox) -> DISEÑADO dentro de §15; FIX de
    render CERRADO (CHANGELOG #13, APLICADO 2026-07-13): Theme.DrawCircle /
    DrawCircleOutlined, primitiva única del theme (draw.RoundedBox no hace
    círculos — radio cuantizado); consumida por sandbox + botón $ + hub del
    wheel.
  - #19 (cinturón de munición) -> FORMA en §15; SEMÁNTICA CERRADA en §16 e
    IMPLEMENTADA (CHANGELOG #11, [APLICADO 2026-07-12], confirmado en juego): el
    cinturón ES la reserva real. Queda abierto: cargadores rellenables con toggle +
    binding de ammo-atts de EFT (§16.6).
  - #16 -> IMPLEMENTADO v1 (CHANGELOG #7, [APLICADO 2026-07-11], confirmado en
    juego): sin pickup por contacto; WALK+USE toma, USE agarra como prop; drops de
    armas = SWEP real. Convar cargo_weapon_world_pickup.
  - #17 -> IMPLEMENTADO (CHANGELOG #10, [APLICADO 2026-07-12], confirmado en juego): reconciliador
    universal en PlayerDroppedWeapon (compat mod "Drop Weapon" — gana sobre la
    nativa; el arma botada ya no queda fantasma en el equipo) + comando nativo
    cargo_drop (bota al frente, conserva la instancia). Antes: PARCIAL (CHANGELOG
    #7, el arma dropeada conservaba su uid; faltaba la tecla de drop).
  - #22 -> CERRADO (CHANGELOG #9 y #17 APLICADOS, confirmado in-game
    2026-07-13): orden 1-7 + holster + manos default; veto del historial
    stock de pickups (cargo_hide_pickup_history); 7.º slot vs HUD D/GL4
    sin conflicto por construcción. Derivó el #36.
  - #18 -> CERRADO (CHANGELOG #10 [APLICADO 2026-07-12]): el cargador persiste
    en el blob vía Inventory.StoreClip.
  - #20 -> SIN diseñar.

16. PICKUP POR MIRAR + USE, no por contacto [Cargo]. Hoy, sin el mod L4D, un arma
    generada por toolgun se recoge SOLA al pasarle por encima (AllowPlayerPickup
    quedó abierto). Objetivo: que NO se recoja por contacto; recogerla requiere
    MIRARLA + USE. Ideal del autor: recoger arma/objeto = combinación walk + use
    (walk para no autorecoger al pasar por encima; use para tomarla como cualquier
    prop) — algo que gmod/hl2 no permiten nativo → plus. Toca corpus_cargo_capture.

17. BOTAR ARMA CON TECLA, conservando funciones de Corpus [Cargo + Caliber].
    [IMPLEMENTADO — CHANGELOG #10, [APLICADO 2026-07-12], confirmado en juego] Reconciliador universal
    en PlayerDroppedWeapon: cualquier ruta de drop (comando nativo cargo_drop, el
    mod "Drop Weapon" YuRaNnNzZZ 946373028, u otro) vacía el slot de equipo y
    taguea la entidad con el uid de la instancia — el arma botada sigue siendo la
    instancia-ítem de Cargo con su blob (no un arma fresca), y el take-back la
    restaura. Decisión del autor: el mod "Drop Weapon" gana sobre la nativa. El
    cargador viaja en el Clip1 de la entidad mientras el arma está en el mundo, y
    se cosecha al blob de la instancia al recapturarla (la persistencia en el BLOB
    la cerró el #18 en la MISMA entry — CHANGELOG #10, [APLICADO 2026-07-12], vía
    Inventory.StoreClip/RestoreClip).

18. PERSISTENCIA DE MUNICIÓN DEL CARGADOR [Caliber semántica + Cargo blob].
    [IMPLEMENTADO — CHANGELOG #10, [APLICADO 2026-07-12], confirmado en juego]
    Si el cargador es 30 y se disparan 15, el arma queda con 15: el estado de munición
    viaja en el blob del ítem (Cargo transporta, el dueño interpreta — contrato §3) vía
    Inventory.StoreClip, y se restaura al equipar. Antes no persistía al capturar/botar/
    reequipar. Prerequisito de 17.

19. SISTEMA DE MUNICIÓN / CARGADORES [Cargo].
    [FUNDACIÓN IMPLEMENTADA — Bloque B, CHANGELOG #11, [APLICADO 2026-07-12],
     confirmado en juego. Diseño cerrado en §16 de Cargo_Architecture.md]
    MODELO (decidido con el autor 2026-07-12, ya construido): EL CINTURÓN ES LA
    RESERVA REAL. Un stack colgado en el cinturón ES el pool nativo del engine para
    su tipo HL2 (SetAmmo/GetAmmoCount); el grid es solo almacén. Las armas del mismo
    TIPO HL2 comparten reserva (como HL2 y STALKER) — la clave del pool es el tipo de
    engine (def.ammo.hl2), NO el calibre (que es etiqueta de display). Lo distinto POR
    ARMA es el CARGADOR (Clip1), que ya persiste en el blob (#18). NO hay reserva
    por-arma con swap. Los 11 tipos base de HL2 son ítems con modelo (verificado contra
    los VPK), peso y max_stack. El espejo cinturón<->pool es un poll a 4 Hz, AGNÓSTICO
    DE BASE: funciona sin un solo hook de ARC9.
    EL ÉTER, MUERTO (era el bloqueante; "la munición no puede aparecer del éter"):
    SWEP:InitialDefaultClip (sh_deploy.lua:130-146) regalaba reserva en cada entrega de
    arma. Tres capas: arc9_mult_defaultammo forzado a 0 (su default es 2) + StripAmmo y
    Push al spawn + reconciliador suprimido hasta ese Push.
    *** HUECO DECLARADO, NO TAPADO (dato que la semilla NO traía): SWEP.ForceDefaultAmmo
    SALTEA esa convar (sh_deploy.lua:140). Hoy ningún arma de los 5 packs instalados la
    usa con valor != 0 (solo las granadas EFT, que la ponen en 0), así que la
    neutralización es completa. Si un pack futuro la reabre, la ESCALACIÓN ANOTADA es un
    ledger de conservación: pool + cargadores debe balancear, y todo excedente sin un
    cargador que lo explique es éter y se clampea. ***
    QUEDA ABIERTO (fuera del v1, confirmado con el autor):
      a) CARGADORES RELLENABLES con TOGGLE (no a todos les gusta llenarlos a mano).
      b) BINDING DE AMMO-ATTS DE EFT (§16.6): los tipos de bala de EFT (FMJ/AP/HP) son
         attachments por calibre que NO cambian el tipo HL2 — solo la balística
         (Penetration, DamageMax/Min, ArmorPiercing...). La palanca: que el stack ACTIVO
         del cinturón decida qué ammo-att va montado en el arma. Da munición realmente
         distinta sobre el mismo pool HL2 — el plus para ARC9 que pedía el autor. El
         campo def.ammo.att ya está reservado en el schema. Referencia: STALKER
         Anomaly / Misery.
      c) Las entidades arc9_ammo/arc9_ammo_big reparten por su propio Touch (no por
         PlayerCanPickupItem), así que el espejo las absorbe al CINTURÓN en vez del grid.

20. CONDICIÓN / MANTENIMIENTO DEL ARMA [Caliber semántica + Cargo condición-blob].
    ARC9 EFT ya traba armas por calor / muchos disparos (sistema propio de ARC9 EFT):
    nuestro sistema puede AUMENTAR la probabilidad de fallo apoyándose en él. Para
    armas ARC9 no-EFT u otras SIN animación/sistema de traba: que la condición eche a
    perder el arma y la deshabilite (no dispara). Lo más cercano a "trabar" sin anim:
    bloquear el disparo como si no tuviera munición. Enlaza con la condición-por-zona
    que Cargo ya guarda en el blob y con el Workbench (reparación); el "qué hace la
    condición" es del dueño del arma (Caliber).

21. QUICKSLOTS DE SANDBOX EN EQUIP: PHYSGUN / TOOLGUN / CAMERA [Cargo UI]. 3 UI
    circulares (solo el símbolo — toolgun, cámara, physgun — sin texto) entre los
    quick slots F1-F4 y el cinturón de munición (orden de §15.2), por ser
    herramientas muy necesarias del sandbox. Toggleable con "hide" en el
    inventario. -> DISEÑADO en §15 y APLICADO (CHANGELOG #8, 2026-07-12).

22. ORDEN DE ARMAS ESTILO STALKER + HOLSTER + matar notificaciones de GMod
    [CERRADO — CHANGELOG #9 y #17, APLICADOS (confirmado in-game
    2026-07-13): orden 1-7 + holster + manos default; veto del historial
    stock (cargo_hide_pickup_history); 7.º slot vs DGL4 sin conflicto.
    Derivó el #36 (alinear slot HL2 con slot Cargo).]
    [Cargo UI/quickslots + compat HUD]. Reordenar las armas de GMod (toggleable):
    1=melee, 2=sidearm, 3=primary, 4=secondary, 5/6/7=physgun/toolgun/camera (el autor
    vio que se puede un 7º slot; verificar compat con el HUD objetivo). Holster
    ("hands"): re-apretar el número del arma actual la enfunda (ej. crowbar en 1 →
    apretar 1 de nuevo vuelve a manos). Referencia: Simple Holster (Chen, 2546335680,
    mapa §2). HAY QUE DESACTIVAR las notificaciones de obtención de armas de GMod
    (ensucian y molestan) — encaja con el feed de pickup propio que ya existe
    (corpus_cargo_pickup). Compat objetivo del autor: "D/GL4 Holographic HUD" (DyaMetR,
    3459525275, GPL-3.0, COMPAT-RUNTIME — mapa §2); verificar que el 7º slot no rompa
    ese HUD.

23. CATEGORÍAS FIJAS PARA LA FILA DE TABS DEL GRID [Cargo UI].
    [CERRADO — CHANGELOG #19, [APLICADO 2026-07-14]: set fijo de 8
    tabs decidido con el autor (All · Weapons · Ammo · Gear · Mods · Meds · Food ·
    Misc), capa de AGRUPACIÓN de display sobre las categorías internas (que quedan
    intactas y siguen sirviendo al grammar "category:a,b"). La fila se dibuja
    siempre entera (tabs vacías atenuadas) y toda categoría no mapeada cae al
    paraguas Misc: nunca vuelve a crecer. Superficie: Items.GetTabs/TabOf/
    MatchesTab. Mapeo completo en Cargo_Architecture.md §7.1.]
    Pedido del autor (2026-07-12, pasada del Bloque A). HOY: el set de
    categorías es ABIERTO — Items.RegisterCategory auto-registra cualquier
    categoría que un def mencione (corpus_cargo_items.lua), así que la fila de
    tabs CRECE sola y hace wrap: "Backpacks" termina en una segunda línea,
    debajo de "All". Se sale de la línea.
    OBJETIVO: NO poblar los filtros dinámicamente. Diseñar ANTES un set FIJO y
    acotado de categorías de DISPLAY (tabs), y mapear las categorías internas
    de ítem contra ellas — p.ej. un tab "Armor" que agrupe helmets + armor de
    Body (y así con el resto), de modo que la fila entre siempre en una línea.
    Es una capa de AGRUPACIÓN de display sobre las categorías de ítem, no un
    renombre: las categorías internas siguen sirviendo a los filtros de slot y
    sub-slot ("category:a,b"), que no deben romperse.
    NOTA: hacerlo bien exige decidir el set fijo primero (sesión de diseño), no
    parchear la fila. Toca §7/§15 de Cargo_Architecture.md.

24. RETÍCULA DEL GRID: SE PIERDE VACÍO, SE CORTA CON POCOS ÍTEMS [Cargo UI].
    [CERRADO — CHANGELOG #14, APLICADO 2026-07-13: la retícula la pinta el
    fondo del viewport (scroll.Paint) desplazada por el offset del canvas —
    cubre el área visible siempre y la fase queda clavada a las celdas; la
    alineación del entry 8 no regresó.]
    Reporte in-game del autor (2026-07-12, 3.ª pasada del Bloque A): con
    el inventario VACÍO el grid no muestra gradas/retícula, y con POCOS ítems la
    retícula se corta en el último ítem en vez de llenar el área visible.
    CAUSA PROBABLE (leer antes de tocar): desde CHANGELOG #8 (ajuste 1 de la 1.ª
    pasada fullscreen) la retícula la dibuja el PROPIO DIconLayout — se hizo así a
    propósito, para que comparta origen con las celdas y scrollee con el contenido.
    Pero el alto del DIconLayout ES el alto del contenido: sin ítems no hay nada que
    pintar, y con pocos termina en el último. Hay que pintarla en un fondo que cubra
    el viewport SIN perder la alineación con las celdas ni el scroll (esa alineación
    fue justo lo que el ajuste arregló — no regresarla). Ojo con la trampa VGUI ya
    documentada: DIconLayout con Dock(FILL) dentro de DScrollPanel colapsa.
    Toca corpus_cargo_grid.lua.


BLOQUE C — UX DE MUNICIÓN (hallazgos in-game 2026-07-12, 1.ª pasada del Bloque B)
---------------------------------------------------------------------------------
El NÚCLEO del #19 quedó confirmado en juego (el cinturón ES la reserva, ARC9 ya no
regala munición del éter, la muerte limpia todo). Estos tres son lo que FALTA para
que el sistema se sienta terminado. Semilla:
ESTADO: CERRADO — entry 12 [APLICADO 2026-07-12], confirmado en juego por el
autor (incluido un intento de exploit de reserva "gratis" rotando munición:
no pasó). La semilla de dev/ se borró al cerrar.

25. REORDENAR EL CINTURÓN [Cargo UI + inventory].
    [CERRADO — CHANGELOG #12, [APLICADO 2026-07-12], confirmado en juego]
    Reporte del autor: "tengo 30 balas de smg en el slot 3 y no puedo ponerlo en el
    ultimo slot del belt". Inventory.BeltSet solo aceptaba refs del GRID (FindEntry
    busca en rec.items), así que mover un stack belt->belt no existía como operación.
    Resuelto con el intent belt_move + Inventory.BeltMove: el swap sin pérdida y el
    merge con tope (max_stack) se REUSARON de BeltSet extrayéndolos al helper común
    BeltMergeInto — no se duplicó la lógica.

26. DESCARGAR EL ARMA (unload) [Cargo + base de arma].
    [CERRADO — CHANGELOG #12, [APLICADO 2026-07-12], confirmado en juego]
    Pedido del autor: poder quitarle la munición al cargador, ejecutando la animación
    de reload. La munición va al CINTURÓN; si el cinturón está lleno, al inventario.
    El espejo del pool (§16.3) ya absorbía lo que vuelve de un cargador (pool sube ->
    cinturón lo absorbe -> excedente al grid); lo que faltaba era el DISPARADOR, hoy
    AmmoPool.UnloadWeapon — intent unload, opción "Unload magazine" en el menú del slot
    equipado y comando cargo_unload. Usa el SWEP:Unload de ARC9 cuando el arma es ARC9
    y el fallback manual (GiveAmmo + SetClip1(0)) cuando no, y llama al StoreClip del
    blob (#18) para que el cargador vacío persista.

27. LOS ÍTEMS BOTADOS SE RECOGEN CON USE PELADO, NO CON WALK+USE [Cargo].
    [CERRADO — CHANGELOG #12, [APLICADO 2026-07-12], confirmado en juego]
    Reporte del autor: "smg rounds del inventario se toman inmediatamente con use". El
    gate de WALK+USE del #16 filtraba por ent:IsWeapon() — SOLO cubría armas — y el
    ENT:Use de lua/entities/corpus_cargo_item.lua recogía INCONDICIONALMENTE, así que
    la entidad de ítem esquivaba el gate entero. El hook PlayerUse de
    corpus_cargo_capture.lua ahora cubre también corpus_cargo_item (USE pelado -> carry
    de prop HL2; WALK+USE -> el hook se aparta y ENT:Use recoge), reusando el debounce
    (CargoNextWorldUse) y el "USE de nuevo suelta" (CargoCarryEnt).

HALLAZGOS IN-GAME 2026-07-12 (pasada del Bloque C) — ESTADO POR ÍTEM
-----------------------------------------------------------------
(Sus cuatro ítems —#28 a #31— están CERRADOS. El marcador de cada ítem manda:
 el encabezado no declara estado por el grupo.)
28. DROP DE ARMAS EQUIPADAS [Cargo UI + inventory].
    [CERRADO — CHANGELOG #14, APLICADO 2026-07-13: opción "Drop" en el menú
    del slot equipado (incl. círculos de herramienta y throwable) → intent
    equip_drop → Inventory.DropEquipped, todo con maquinaria existente; las
    cuatro formas confirmadas en juego.]
    Reporte del autor: "falta un
    drop weapon de armas equipadas". Hoy el camino es unequip al grid y drop
    desde ahí, o cargo_drop con el arma en la mano. Candidato natural: opción
    "Drop" en el menú contextual del slot equipado, reusando la maquinaria de
    DropEntry/SpawnWorldWeapon para que el arma salga como SU MISMA instancia
    (#17) con el cargador persistido (#18).

29. COLOR DE LA UI: NEUTRO ESTILO SPAWNMENU + INTEGRACIÓN DGL4 [Cargo UI].
    [CERRADO — CHANGELOG #14, APLICADO 2026-07-13 → §15.5 de la arquitectura:
    paletas runtime que mutan T.Colors en sitio; base spawnmenu default,
    cargo_theme olive restaura la GAMMA; teñido DGL4 del preset activo con
    re-tint en vivo (OnSettingsChanged). El wheel nació listo para el tinte
    (cero colores horneados).]
    Pedido del autor: el color base de Cargo debe ser el del spawnmenu/browser
    de GMod — neutro, "listo para integrarse con cambios benignos sobre el
    spawnmenu" — y con el HUD DGL4 montado la UI debería teñirse según su preset
    activo (el autor usa el PCV de Opposing Force / Foxtrot uniform; mod nuevo
    en dev/other/: "[dgl4] official presets pack"). COMPAT-RUNTIME: el color se
    LEE de la API/convars de DGL4 en runtime, jamás se asume — consultar
    ../dev/mods_workshop_mapa.md y el código vivo antes de diseñar. Toca
    corpus_cargo_theme.lua (única fuente de estilo — el resto de la UI ya bebe
    de ahí).

30. SPAWNMENU: EL ÍCONO DE ARMA NO LLEGA AL INVENTARIO [Cargo capture].
    [CERRADO — CHANGELOG #14, APLICADO 2026-07-13: el hook PlayerGiveSWEP
    (sandbox commands.lua:940) marca el give del spawnmenu como deliberate
    (ventana 1 s), así que el arma clickeada llega al inventario.
    ENMENDADO — CHANGELOG #27 (pedido del autor 2026-07-14): al ser DELIBERADO,
    el click del spawnmenu sobre una clase ya poseída CAPTURA la N-ésima copia
    (cada arma es su propia instancia; el comercio ya las vendía por separado).
    El dedup quedó acotado al give ANÓNIMO — ver Capture.Decide en
    server/corpus_cargo_capture.lua. El aviso "You already have one." YA NO
    existe en el código: es historia, ver #15.]
    Reporte del autor: sin el toolgun no hay forma de obtener physgun/toolgun/
    camera; al clickear el ícono del arma en el spawnmenu, el arma no llega al
    inventario. Sospecha (NO confirmada): el filtro de loadout de sandbox del
    entry 8 (cargo_capture_sandbox_tools 0) o el flujo de give del spawnmenu
    esquivan la captura — diagnosticar leyendo capture.lua ANTES de arreglar.

31. WHEEL MENU [Cargo UI].
    [CERRADO — CHANGELOG #13, APLICADO 2026-07-13: diseño bajado de Claude
    Desktop (prompt semilla del wheel, mock congelado v2_1), implementado en
    client/corpus_cargo_wheel.lua y verificado en juego en dos pasadas + el
    re-chequeo visual. Arquitectura → §17: cero server nuevo (intent slotkey
    + 8 wheel-only del throwable), hub de información universal con las
    rutas ARC9 verificadas (#33), chips quick/tools con anclajes
    configurables, HUDPaint blindado con pcall.]

=====================================================
Diseño de referencia: Cargo_Architecture.md (inventario, Block 1 + §15 UI fullscreen),
Cargo_ItemImages_Arquitectura.md (imágenes de ítems), Cargo_Trade_Arquitectura.md
(comercio) y Workbench_Arquitectura.md (banco, bloque futuro).
Metodología: ../../corpus/docs/corpus_flujo_trabajo.txt.
=====================================================

HALLAZGOS IN-GAME 2026-07-13 (2.ª pasada de los entries 13/14) — SEMILLA:
../../dev/HANDOFF_cargo_pendientes_pasada2.md
-----------------------------------------------------------------
32. TAXONOMÍA DE GRANADAS + GATE DE CAJAS DE MUNICIÓN [Cargo ammo/items].
    [CERRADO — CHANGELOG #16, APLICADO 2026-07-13 (confirmado in-game): la
    cara canónica de Grenade/slam es el lanzable (cargo_throw_frag/slam), el
    espejo absorbe al slot/grid (§16.9), la captura no acuña wpn_weapon_frag,
    ids viejos remapean (LegacyThrowIds), y las cajas item_ammo_* van por
    WALK+USE en el gate único de capture.lua (veto puro).]
    Reporte del autor: el frag de HL2 se genera como ítem de MUNICIÓN
    (cargo_ammo_grenade, cara canónica del tipo "Grenade" desde el Bloque B)
    al morir/spawnearlo — debería ser LANZABLE (categoría throwables, slot);
    la granada del SMG1 sí es munición. Decidir la cara canónica de
    "Grenade"/"slam" (¿throwable en vez de ammo de cinturón?) y qué hace
    AbsorbType con esos tipos. ADEMÁS: las cajas item_ammo_* spawneadas con
    click medio se toman por CONTACTO (diseño del Bloque B en
    PlayerCanPickupItem, ammopool.lua) — el autor quiere WALK+USE también
    ahí; reusar el patrón del gate de capture.lua.
33. HUB ARC9 INCOMPLETO EN EL WHEEL [Cargo UI].
    [CERRADO — CHANGELOG #16, APLICADO 2026-07-13 (confirmado in-game):
    AmmoInfo con la ruta del Ammo1 de ARC9 + fallbacks SWEP.Ammo/
    GetLoadedRounds; defs autogen con def.ammo.caliber (etiqueta del pool) +
    derivación runtime (CaliberOf). Arquitectura → §17.5.]
    Muestra "Auto · Group A":
    falta el calibre (defs autogen wpn_<clase> sin def.ammo — poblarlo al
    capturar o leer de ARC9 en runtime) y cargador/reserva (AmmoInfo de
    corpus_cargo_wheel.lua devuelve nil con SWEP válido; FiremodeOf sí
    responde — diagnosticar contra el ARC9 vivo qué devuelven Clip1 /
    GetProcessedValue("Ammo") / GetPrimaryAmmoType client-side).
34. VELOCIDAD PISADA POR EL MOD DE MOVIMIENTO [Cargo movement, COMPAT].
    [CERRADO — CHANGELOG #16, APLICADO 2026-07-13 (confirmado in-game): hook
    Move shared (corpus_cargo_movecompat.lua) que escala el MaxSpeed del
    move data con el mult publicado por NW2Float; gated por sv_bm_enabled y
    cargo_movement_compat; vanilla intacto sin el mod. Arquitectura → §5.]
35. FOOTSTEPS MUDOS AL TOGGLEAR EL MOD DE MOVIMIENTO [COMPAT, lado mod] —
    SIN ARREGLAR. Reporte del autor (2026-07-13, pasada del entry 16): con
    sv_bm_enabled 0 los pasos quedan en silencio, y el remedio hipotetizado
    (sv_bm_slow_footsteps 0) NO funcionó — la sospecha del math.huge en
    PlayerStepSoundTime (better movement v2, sh_bm_main.lua:492-496) queda
    NO confirmada. La pata de compat de Cargo (#34) no toca rutas de sonido:
    es comportamiento del mod, pero molesta al testear. Investigar CON EL
    MOD VIVO en juego (¿qué queda enganchado tras el toggle? ¿recupera con
    respawn/mapa?) antes de decidir si Cargo media o se documenta como
    limitación del mod. COMPAT-RUNTIME: jamás forkearlo.
    Identificado por el autor: "better movement v2 with sprint step time"
    (dev/other/) sobreescribe walk/run y anula la curva peso→velocidad.
    El footer muestra bien. Leer el mod vivo y decidir la palanca
    (re-aplicar, hook de Move, o detectar su API) — jamás forkearlo.

36. ALINEAR EL SLOT DEL MENÚ HL2 CON EL SLOT CARGO [Cargo UI/quickslots,
    compat] — SIN DISEÑAR. Pedido del autor (2026-07-13, pasada del entry
    17c): hay armas que declaran otro Slot de engine (la RPD de EFT es Slot
    4) — equipada como primary debería vivir en el bucket 3 del menú por
    rueda del mouse (HL2 o DGL4), coherente con la tecla 3 de Cargo. Palanca
    candidata: pisar SWEP.Slot (lo que lee weapon:GetSlot(); DGL4 bucketiza
    por GetSlot()+1) al equipar/dar el arma según el slot Cargo del record,
    y restaurarlo al dropear. Verificar contra el mod vivo si ARC9 relee
    Slot en runtime y en qué realm hay que escribirlo.

37. ARMAS VJ: EL DROP VUELVE SOLO AL INVENTARIO [Cargo capture, COMPAT] —
    HECHO y CONFIRMADO EN JUEGO 2026-07-24 (CHANGELOG #36-#40; saga completa).
    Reporte del autor (2026-07-13, pasada del entry 4c): las armas de VJ Base
    se toman UNA sola vez con USE, entran bien al inventario, pero al
    dropearlas la re-captura es instantánea (el arma vuelve al inventario y
    no queda en el piso), regalando munición en cada ciclo.
    DIAGNOSTICADO contra el mod vivo (2026-07-24): el hook
    PlayerCanPickupWeapon de VJ (vj_base/hooks.lua:354) autoriza CUALQUIER
    pickup de un arma VJ con menos de 0,15 s de vida (ventana InitTime para
    sus gives de NPC) y en hook.Call el primer retorno no-nil gana — el
    world gate de Cargo nunca vetaba; la munición gratis era el
    GiveAmmo(ClipSize*2) del SWEP:Equip de VJ (shared.lua:411).
    RESUELTO en SpawnWorldWeapon: retro-fechar wep.InitTime en armas VJ —
    el drop nace "en reposo", el propio hook de VJ niega el contacto y el
    WALK+USE deliberado entra por su rama KeyPressed(IN_USE). De paso la
    familia entera se preció: weapon_vj_* a $200 plano vía
    Capture.WeaponValuePrefixes (mismo parche, PARCHE 2).
    CIERRE (CHANGELOG #37, tras confirmar la #36): el hook de VJ respondía
    ANTES que el world gate (autorun vs Initialize) y siempre no-nil — USE a
    secas / autopickup / ventana InitTime capturaban sin WALK+USE. Se
    re-asienta LA MISMA función detrás del gate (takeover de orden, patrón
    QL), el drop de cadáver de NPC (OwnerIsNPC estampado) se niega desde el
    frame uno, y el regalo de ClipSize*2 del SWEP:Equip se devuelve en cada
    adquisición (nuestros equips del grid incluidos).
    CIERRE 2 (CHANGELOG #38): las armas MadeForNPCsOnly regalan la munición
    ANTES de auto-borrarse y el equip abortado nunca llega al clawback — la
    toma WALK+USE las rechaza antes del pickup, respeta
    vj_npc_wep_ply_pickup 0 (revertido el pisado de la #37), nunca se acuñan
    como ítems y las ya acuñadas se purgan al spawn.
    CIERRE 3 / FORMA FINAL (CHANGELOG #39): el re-asiento de la #37 perdió
    la lotería de orden — hook.Call entre hooks DISTINTOS no es orden de
    inserción y el KeyPressed de VJ seguía autorizando USE a secas. La
    función de VJ corre ahora EMBEBIDA en el world gate (un solo hook: VJ
    manda solo donde el gate se abstiene — ventana de give, gate apagado) y
    el deny de NPC-only sube al gate (ninguna ruta, ninguna edad).
    LECCIÓN para futuros takeovers: si el orden entre dos hooks importa, el
    segundo se embebe en el primero — jamás se re-registra aparte.
    REMATE (CHANGELOG #40): el clawback neteaba el regalo pero el popup del
    engine ya había disparado (history de DGL4 con "+60 SMG1") — el gate,
    choke point de todo pickup, neutraliza el regalo ANTES de que exista:
    copia propia de Primary con PickUpAmmoAmount = 0 en la instancia (nunca
    a través de wep.Primary: mutaría la tabla de la clase). Prevenir > devolver.

38. TRIVIA REAL PARA ARMAS CAPTURADAS [Cargo UI] — HECHO (CHANGELOG #23,
    [APLICADO 2026-07-14]).
    Idea del autor (2026-07-13, pasada del entry 15c): el "autocaptured" del
    footer del tooltip podría moverse a la trivia estilo ARC9, y/o generarse
    trivias con datos reales (EFT/GAMMA) para el arsenal del volcado de
    cargo_dev_dump_weapons.
    RESUELTO sin tabla: la FUENTE es el SWEP mismo. ARC9 escribe SWEP.Description
    (string ya localizado al cargar el archivo) y un bloque SWEP.Trivia
    (fabricante/calibre/acción/país/año), y weapons.Get(class) los entrega ya
    heredados por la cadena SWEP.Base. La captura los lee y los pone en
    def.trivia + def.trivia_rows; el tooltip pinta una sección "Specs" bajo los
    stats. Cualquier pack ARC9 que se monte después queda cubierto solo.
    corpus_cargo_weapon_trivia.lua es la EXCEPCIÓN (40 entradas): huecos que el
    pack no escribió, herencias mentirosas (el M16A1 hereda de arc9_eft_m4a1 y
    se presentaría como un M4A1) y las armas HL2, que no son SWEPs.

39. SLOT POR TIPO DE ARMA [Cargo capture/slots] — HECHO (CHANGELOG #24,
    [APLICADO 2026-07-14]).
    Reporte del autor (pasada de la entry 23): un RPG podía equiparse en
    Sidearm. Los tres slots de arma filtran por category:weapons, así que
    cualquier arma entraba en cualquiera; def.equip_slots (el gancho para
    estrechar) existía desde el Block 1 y el def autogen nunca lo usaba.
    RESUELTO derivando del propio SWEP (no catalogando): SWEP.Class es por arma
    y SWEP.SubCategory por pack, y weapons.Get() resuelve ambos por la cadena
    SWEP.Base — un pack que nunca vimos se clasifica solo. Pistolas -> sidearm;
    largas (fusil/SMG/escopeta/LMG/sniper/lanzador) -> primary+secondary; melee
    -> categoría melee; sin clasificar -> libre (degrada, nunca inequipable).
    ReconcileEquipSlots saca al spawn lo que quedó en un slot que ya no lo
    acepta.
    DIFERIDO dentro de este frente (decisión del autor 2026-07-14: "dejarlas
    como están por ahora"): las GRANADAS de ARC9 quedan clasificadas ("thrown")
    pero SIN restringir — son ítems unique y el slot Throwable pide un stack de
    stackable (§4), así que siguen equipándose en Primary/Secondary. Convertirlas
    es territorio del #32. El clasificador ya las etiqueta: accionarlo es una
    línea en KIND_SLOTS, no un rediseño.

40. PESOS QUE CAEN AL NOMINAL DE 2,5 KG [Cargo capture, datos] — HECHO
    (CHANGELOG #25, [APLICADO 2026-07-14]). Sigue pendiente de verificar SOLO su
    addendum (el dump dejó de gritar lobo: un cargo_dev_dump_weapons nuevo debe
    encabezar con "sin peso: 0 | sin precio: 0", y las plantillas de SWEP salir
    n/a, no MISSING).
    El volcado del autor tenía 369 SWEPs y 184 sin peso: packs EFT que no
    están en dev/other/ (SMG, escopetas, LMG, melee, gear, makeshift), el pack
    entero de CS:GO (arc9_go) y el Scorpion de arc9_wtt. El volcado alcanzó
    para catalogarlos SIN tener el pack (trae clase/nombre/tipo/munición/
    cargador). Resultado: 360 armas vivas, las 360 con peso Y precio, cero
    huecos. El M60E4 pesa 10,5 kg. Las clases BASE no se catalogan (son
    plantillas de SWEP, no armas). Registro original del frente abajo.
    Reporte del autor (2026-07-14): el M60E4 pesa 2,5 kg. Es de un pack ARC9
    que NO está en dev/other/, así que no entró en weapon_weights. No es un bug
    de código: es un hueco de datos, y el fallback funcionó como se diseñó.
    Herramienta de diagnóstico ya existente: cargo_dev_dump_weapons vuelca
    clase/nombre/tipo/munición/cargador/peso a data/corpus/cargo/weapon_dump.txt
    y marca cada hueco como "MISSING (2.5 nominal)". Falta el volcado del
    arsenal real del autor para poblar weapon_weights + weapon_prices.

41. EXPLOSIVOS ARC9 COMO STACK THROWABLE [Cargo items/slots] — ABIERTO,
    bloque propio (decisión del autor 2026-07-14: "amerita hacer un bloque
    especial para mejorarlo, pero eso después").
    Hoy las granadas de EFT, CS:GO y MW2019 se equipan en Primary/Secondary
    como si fueran armas largas. El clasificador del #39 YA las etiqueta
    "thrown"; lo que falta es el destino: son ítems unique con su propia
    entidad SWEP, y el slot Throwable toma un STACK de un stackable (§4,
    enmienda del wheel), con su ×N y su espejo al cinturón (§16).
    Convertirlas toca: clase del def (unique -> stackable), el pool de
    munición, el cinturón y el commit del wheel. Enlaza con el #32 (la
    taxonomía de throwables que ya cubre frag/SLAM de HL2).
    Cuando se accione, el gancho ya está: una línea en KIND_SLOTS.

42. LANZAGRANADAS CAPTURADO SIN ATTACHMENT DE MUNICIÓN [Cargo arc9, bug]
    — ABIERTO. Reporte del autor (check 4 de la entry 24, 2026-07-14): el
    M32A1 se equipa bien en Primary pero NO puede disparar; "el attachment de
    ammo del arma se perdió". Dejado como anotación, no como bloqueo.
    Sospecha: el puente ARC9 (§10). Cargo es el almacén de attachments y
    fuerza arc9_free_atts 0 (takeover), así que un arma que NACE con un att
    de munición integral se queda sin él: el att no está en el inventario del
    jugador, y PlayerGetAtts responde 0. Diagnosticar contra el mod vivo:
    ver si el GL declara su ammo como att Free (que el puente ya excluye) o
    como att normal, y si el default loadout del SWEP lo instala por fuera de
    los hooks de inventario.

43. ÍTEMS/ARMAS FAVORITOS [Cargo UI] — CERRADA el 2026-08-21, el mismo día que
    se escribió. Acuña CRG-76, sede Cargo_Architecture.md §7.4.
    PASADA EN JUEGO OK, 13/13 EN UNA RONDA: planilla AI
    (dev/checks/cargo-favoritos-r1.html), PASA 13 · FALLA 0 · SIN CORRER 0.
    CHANGELOG 76 [APLICADO 2026-08-21].
    SE ACREDITA GESTO POR GESTO Y NO POR COBERTURA, y es lo que la distingue de
    la #75: nueve de las trece filas traen la evidencia concreta en la nota. Las
    CINCO puertas se ejercieron una por una, y el rechazo de vender salió POR
    ÍTEM y con el nombre adentro («Backpack is a favorite: unmark it before
    selling it», ídem «Large Backpack» y «RSh-12») — o sea que el gate resuelve
    la ENTRADA y no la sesión.
    ⭐ EL MERGE CERRÓ CON LA ARITMÉTICA A LA VISTA, y era la fila que decidía
    más que las otras doce: «tengo 4 vendas en favorito, una en el suelo; al
    tomar tengo 5, o sea se stackeó y no se puede botar ahora porque las 5 están
    en favorito». 4+1=5 y las cinco marcadas — que es exactamente lo que la
    clave por clase promete y lo que un flag por celda no podía dar sin
    partirse en dos mitades sin regla. La decisión del autor quedó medida EN
    JUEGO y no sólo sobre el record.
    EL FALSO ROJO QUE SE HABÍA PREDICHO NO OCURRIÓ, y la respuesta fue de
    lectura de código y no de otra ronda: la nota del «Move all» trae los dos
    avisos juntos —capacidad y favoritos— pero el gate vive ANTES del loop que
    prende `blocked`, así que un favorito no puede alcanzar esa bandera; el de
    capacidad reportó un bloqueo real sobre ítems NO favoritos. Queda anotado
    porque el próximo que vea esos dos renglones juntos va a sospechar del
    candado.
    Y SU HALLAZGO SALIÓ DE LA NOTA DE UN CHECK QUE PASÓ (AI5), octava vez en el
    arco: dentro de la ventana de loot el grid del jugador no ofrece ni marcar
    favorito ni botar. Es el #76 y NO un defecto de esta entrada.
    Pedido del autor 2026-07-23 (nota de la verificación
    de la entry #27), DISEÑADO POR ÉL el 2026-08-20 y con sus cuatro decisiones
    votadas el 2026-08-21.
    QUÉ PEDÍA, textual: «el ser favorito significa que no lo puedes vender ni
    dropear […] no puedes mandarlo al loot box sin quitarle el favorito […] que
    no pierdas los objetos que marcaste como favoritos, porque en el juego hay
    muchas armas que te pueden confundir y en una de esas puedes terminar
    vendiendo la tuya que siempre usas».
    EL VALOR DE LA ENTRADA NO ES QUE EL CANDADO EXISTA: ES QUE NO FALTE NINGUNA
    PUERTA. Él nombró tres cosas; en el árbol eran CINCO sitios más el clic del
    cliente — vender (commit + clic), «Move all», transferencia individual, drop
    del grid y drop de EQUIPO. Una sola casa (Inventory.IsFavorite) y un gate que
    cuenta los SITIOS DE LLAMADA por archivo: sabotear el helper pone todo rojo,
    pero devolver UNA puerta a su código de antes dejaría la pasada verde.
    Acuñó CRG-76, sede Cargo_Architecture.md §7.4.
    LA DECISIÓN MÁS CARA, y es del autor: un favorito de algo que APILA es POR
    ÍTEM y no por celda (unique -> uid, stackable -> id). AddStack MERGEA SOLA,
    así que un flag por celda queda INDEFINIDO en cuanto levantas otro del piso,
    sin un solo error — y arreglarlo obliga a tocar el merge, que es la #73.
    Costo declarado: no se pueden tener 3 vendajes favoritos y 2 no.
    LA MUNICIÓN EXCLUIDA es voto suyo con su motivo («la data de la municion es
    muy cambiante»), y por eso BeltDrop (#72) NO puede ser una sexta puerta.
    LA UI SALIÓ TOGGLE Y NO NOVENA TAB, con el ancho MEDIDO antes de preguntar:
    las ocho tabs de hoy suman 454 px contra los 424 de la barra a 1280x720, o
    sea que `Misc` YA wrapea — y acortar el label a «Fav» no cambia el resultado
    en NINGUNA resolución. Lo que decide es la cantidad de tabs, no el largo del
    nombre. El comentario del código que afirma que el set fijo entra en una fila
    «at every aspect we ship» es FALSO en las dos resoluciones más chicas.
    LA ESTRELLA SE DIBUJA Y NO SE TIPEA, y es una medición: Roboto no tiene
    glifo para U+2605 (verificado con fontTools sobre el .ttf de GMod), así que
    un draw.SimpleText pintaría el cuadrito de glifo faltante. Va con DrawPoly
    abanicando desde el CENTRO — una estrella es cóncava y el abanico sale del
    vértice uno.
    PENDIENTE: la pasada en juego (planilla AI, 13 filas).

44. OVERLAY DE MÁSCARA DE GAS (EXTRA DEL BANCO DE SONIDOS) [Cargo UI/equip] —
    ABIERTO (pedido del autor 2026-07-24, al ordenar el banco de sonidos).
    Cuando el jugador se equipe un casco CERRADO (el def lo declara — p.ej.
    flag `def.gasmask`, a diseñar), un overlay de pantalla que funcione como
    máscara de gas + la respiración muffled del banco
    (corpus/sound/corpus/cargo/gasmask/): gas_breath_N_x viene en INTENSIDAD
    1..8 — la 8 es "sin stamina o muy mal herido" en GAMMA; candidata natural
    a leer stamina/estado de Coagulant por detección (COR-5), con fallback a
    intensidad fija sin él. mask_clean.ogg queda para el mantenimiento futuro.
    Los sonidos ya están ordenados y documentados (about.txt de la carpeta);
    SIN DISEÑAR: overlay (material/viñeta), gatillo exacto (qué cascos), y el
    mapeo intensidad→estado.

45. TRADE ADOSABLE A CUALQUIER ENTIDAD POR ADMIN [Cargo trade] — ABIERTO
    (wanted idea del autor, 2026-07-24, al estrenar el trader NextBot de
    corpus-stalker). Para CÓDIGO la superficie ya existe y es el contrato:
    `Trade.AttachTrader(ent, opts)` + `Trade.OpenFor` sobre cualquier entidad
    (el NextBot de stalker es la prueba viva). Lo que falta es la ruta ADMIN:
    marcar EN JUEGO un NPC/entidad arbitraria como trader sin escribir Lua.
    SIN DISEÑAR — decidir: vehículo (¿toolgun/comando?), persistencia del
    attach entre sesiones (¿persistKey contra Corpus.Data?), y de dónde salen
    stock/spread/billetera (¿preset dev, editor?). Anotado para que la idea
    no se pierda; enlaza con el estándar de voz por carpetas de acción de
    corpus-stalker (el NextBot subclaseable es la otra mitad de "traders
    hechos por usuarios").

46. LISTA DE FUENTES DE LUZ EN EL WHEEL [Cargo ui/arc9] — HECHO 2026-07-29,
    entries 52-53 [APLICADO], confirmado en juego con la PLANILLA V en 10/10
    tras CINCO rondas. La ronda 1 se perdió entera por el entorno (la G del
    autor era wheel Y `impulse 100`, y como el wheel pollea la tecla el toggle
    daba NETO CERO); las rondas 2-5 encontraron tres defectos y una afirmación
    falsa del doc, y CUATRO de los cinco hallazgos salieron del campo de NOTAS
    de checks que PASARON, no de un rojo.
    LOS TRES DEFECTOS SON EL MISMO ERROR CON DISTINTO DISFRAZ — una API
    asumida en vez de medida: FlashlightIsOn supuesta legible en el cliente
    (no lo es; ply:Flashlight es server-only en las DOS direcciones, y por eso
    el chip nunca pintaba ON — el remedio es un espejo NW2Bool que lee el
    ENGINE y no nuestros commits), UnequipDelay supuesta ausente en todas las
    variantes (las 12 aviators la declaran en 0,25 — el CÓDIGO nunca compartió
    el error, solo la prosa y el stub que la había copiado), y
    EnableScreenClicker supuesta idempotente (no lo es: llamarla por frame
    re-inicializa el clicker y el cursor parpadea). La tercera se cometió
    ESCRIBIENDO EL PARCHE QUE DOCUMENTA LAS DOS PRIMERAS. CRG-24 manda
    verificar las APIs de terceros contra dev/other/, y EL ENGINE TAMBIÉN ES
    UN TERCERO — el que más fácil se olvida que lo es.
    Frontera declarada en §13: con un arma ARC9 con dispositivo desplegada,
    ARC9 SUPRIME EL RENDER del haz de la linterna del engine aunque el estado
    siga en true (vuelve al cambiar de arma). No se pelea.
    Ejecutado 2026-07-28.
    La norma presupuestada se acuñó (CRG-64: un estado asíncrono de un
    tercero pinta el TRÁNSITO) y CRG-30 quedó ENMENDADA (un único intent de
    toggle sin payload; el titulo de ids.yaml corregido en el mismo parche).
    El mock gobernó la forma: mockups/cargo_wheel_lights_mock_v1_1.html,
    bloques 01-05 implementados. La bifurcación de las barras de modo se
    resolvió A FAVOR: GetFinalAttTable fusiona el modo activo, los emisores
    son DATOS y nada se parsea.
    TRES DIFERIDAS DEL MOCK — APROBADAS POR EL AUTOR (2026-07-27), NO
    propuestas; sede: el propio mock. Aprobado-y-sin-anotar es como se
    pierde un diseño:
      (g) VARIANTE (B) — celda ancha 150x56 solo para el grupo de luces
          (bloque 06). Configurable, solo anclaje izquierda/derecha, quick y
          tools siguen cuadrados, degrada a 56x56 en eje horizontal.
          Convierte la constante de celda en parametro (w,h) por grupo y
          hace convivir dos anatomias de celda — GroupCells hoy no se toca.
          EJECUTADA en el #48 (entry 54, [PENDIENTE]): GroupCells toma (w,h)
          y la regla salio como pura, GroupCellSize, que CLAMPEA w a h en el
          eje horizontal — escrita asi, la degradacion no es un caso especial
          en ninguna parte: ES el clamp. La advertencia de desborde del mock
          quedo CORREGIDA con la cuenta (entra en 1280x720), fijada en el
          harness y sin editar el mock.
      (h) FADES (90 ms abrir / 200 ms cerrar), CICLO DE VIDA DE CIERRE y
          estado BLOQUEADO de rechazo (tramado rojo + parpadeo + cue sonoro,
          bloque 07). Toca el dibujador RAIZ del wheel (alpha global por
          frame y ~200 ms pintando con el input consumido) — la superficie
          que V6 declara intacta. ARRASTRA UNA VERIFICACION PENDIENTE
          (CRG-24, contra dev/other/): si el mod de NVG realmente bloquea la
          linterna durante su animacion — su premisa, hoy NO medida. El
          rechazo de la tanda ejecutada es MUDO.
      (i) BATERIAS / condicion 0-100 de las luces (bloque 08). Preview sobre
          el modelo GAMMA (la carga vive en el pack, el dispositivo la
          refleja); sin codigo detras, con dos cosas que podrian tirarlo
          abajo declaradas (consumo por modo, pack compartido).
    ------------------------------------------------------------------
    Lo de abajo es el diseño con que se ejecutó, se conserva como historia:
    PROMPT: dev/PROMPT_cargo_tls_luces.txt (v1, 2026-07-27) —
    entradas 52-53, planilla V, UNA NORMA PRESUPUESTADA (un indicador de un
    estado ASÍNCRONO pinta el TRÁNSITO, hermana de CRG-32 en el eje del
    tiempo; su ID se acuña en el parche que la escribe, no acá — presupuestar
    no es acuñar, y el checker de IDs lo hace cumplir) y la ENMIENDA a CRG-30,
    cuyo texto de entonces ("cero mensajes de red nuevos") dejó de ser cierto
    con el net de la linterna.
    LA TRAMPA QUE EL PROMPT PONE EN SU PRIMERA PÁGINA: el doc de diseño se
    escribió ANTES de #47, cuando la NW "nvg" no la escribía nadie más. Hoy
    la escribe CARGO (server/corpus_cargo_nvg.lua), así que el chip solo
    puede togglear "nvg_on" — escribir "nvg" sería fabricar la segunda ruta
    que este repo evita. Y sh_arctic_nvg_losegoggles ya está en 0: lo puso
    #47 y no se vuelve a tocar.
    (Pedido del autor 2026-07-26, tras mapear el mod [TLS] Tactical Light Sources).
    El dolor concreto: la G es a la vez el wheel de Cargo y la linterna del
    autor. La solución es meter las luces DENTRO del wheel, que ya es hold.
    TERCER GRUPO DE CHIPS (no un anillo nuevo: el anillo es "equipar", los
    quick son "usar", las luces son "togglear" — mezclar verbos en un mismo
    anillo ya está declarado error de UX en §17.6), en COLUMNA vertical,
    convar cargo_wheel_lights_anchor, default left.
    REGLA DE COLISIÓN pedida por el autor, y distinta a la de §17.6: las
    luces SÍ pueden compartir lado con quick o tools, EMPUJADAS unas unidades
    hacia afuera. ResolveAnchors no se toca (es pura y la cubre el harness);
    se le suma una hermana pura, LightsPushOut, con aviso único por
    Corpus.Log — jamás un error.
    TRES FUENTES, ninguna inventada (CRG-32): linterna del jugador ·
    NVG de Neosun (concommand cliente arc_vm_nvg, estado NWInt "nvg" /
    NWBool "nvg_on", con el EquipDelay de ~1,3 s pintado como tránsito) ·
    dispositivos ARC9, uno por slot toggleable (GetSubSlotList → ToggleStats
    + ToggleOnF; commit = ToggleStat(addr) + PostModify(), client-side, que
    replica solo — el MISMO contrato de CRG-23). Fuera: TFA (§10.4), y el
    chemlight / los perfiles de linterna extendida (sus addons no están
    instalados). Para que eso no sea techo, la lista SE REGISTRA
    (RegisterLightSource, patrón de StatusPanel.RegisterBar §11).
    DECISIÓN DEL AUTOR (2026-07-26): la opción (b), UN net sin payload
    (Corpus.Net.Register("cargo","torch")), seis líneas de server y sin
    estado. Se paga la enmienda explícita a CRG-30 en §17.1 — el commit del
    wheel deja de ser sólo el intent `slotkey`: también puede ser un TOGGLE.
    Se descarta la (a), RunConsoleCommand("impulse","100"): no cuesta norma
    pero ARC9 SECUESTRA ese impulso cuando el arma en mano tiene un
    dispositivo toggleable (sh_move.lua:360-369), o sea la misma tecla hace
    cosas distintas según lo que lleves. Un comportamiento que depende de lo
    que tengas en la mano es peor que una enmienda de norma escrita.
    NUNCA devolver true a "impulse 100" en PlayerBindPress: eso es
    justamente lo que TLS hace y lo que le rompe el toggle de atts a ARC9.
    Diseño completo, API verificada contra el código vivo y defectos del mod
    inventariados en dev/Cargo_TLS_Referencia.md §9. Assets: TRES íconos
    propios (linterna, NVG, dispositivo genérico — lo cambió el mock: nada
    sale de atttbl.Icon), montados en materials/corpus_cargo/wheel/.

47. NVG DE NEOSUN COMO ÍTEMS DEL INVENTARIO [Cargo items/capture] — CERRADO
    2026-07-27, entries 50-51 [APLICADO], planilla U en 6/6 (dos pasadas).
    LA 2.ª PASADA LA DISPARÓ EL USO, NO UN CHECK EN ROJO: la planilla midió
    lo que se propuso medir y lo que faltaba —extraer un sub-slot con el host
    en la MOCHILA, acoplar arrastrando, y el ícono autogenerado en vez del
    PNG del mod— apareció al jugar con lo que ya había dado por bueno. Es la
    contracara de la lección de B3/B4/B5 (el hallazgo sale del campo de notas
    de un check que PASÓ): una planilla mide lo que se propuso medir; el uso
    mide lo que falta.
    Frontera declarada y medida (§13): apagar cargo_nvg_register con gafas ya
    en el inventario las deja huérfanas de def y se destruyen al dropearlas —
    aceptado, el kill-switch es para un servidor que NUNCA montó el catálogo.
    La otra mitad de ese mismo check vale más: con la convar en 0 el mod
    recupera su comportamiento ENTERO, o sea COR-5 medido y no declarado.
    Lo que la tanda cambió respecto de lo anotado acá abajo, y hay
    que decirlo porque salió de re-greppear el árbol y no de leer el diseño:
      · LAS PUERTAS DE LA SEÑAL SON CINCO, NO CUATRO. DropEquipped NO pasa
        por Unequip (manipula rec.equip y difunde por su cuenta, como el
        BodyChanged existente ya hacía en sus dos salidas), y el
        reconciliador de WeaponDrop de capture.lua vacía el slot al soltar
        el arma en la mano. La de DropEquipped tiene consecuencia visible:
        dropear el casco con las gafas montadas dejaría al jugador viendo a
        través de gafas que ya no tiene.
      · RegiveEquipped difunde con slotId nil = "se re-aplicó todo": es la
        puerta del respawn, y evita que el oyente enganche PlayerLoadout y
        apueste al orden de hooks. No se escribió un segundo re-give.
      · Corpus_Cargo_BodyChanged NO tiene consumidor hoy (grep en las siete
        raíces): Cortex todavía no tiene código. Su firma se respeta igual —
        es contrato declarado del roadmap #10 — pero la nota de acá abajo
        ("Cortex cuelga de él") era una previsión, no un hecho.
      · La degradación del ícono es a la LETRA, no al render de modelo:
        ResolveIconSource devuelve "icon" apenas def.icon es un string no
        vacío. Inalcanzable en la práctica (el mod que trae la tabla trae los
        67 PNG, contados), pero la afirmación del doc de diseño está mal.
    Normas acuñadas: CRG-62 (sede Cargo_Architecture.md §4) y CRG-63 (sede
    §3 — se eligió §3 sobre §13 porque §13 es la tabla de deuda declarada y
    una norma vigente no vive en una lista de pendientes).
    (Pedido del autor 2026-07-26, tras mapear [VManip] Neosun's Cooler
    Nightvision). Es la OTRA MITAD de #46: aquel ACCIONA la visión nocturna
    desde el wheel, este la POSEE — recogerla del mundo, guardarla, equiparla.
    Cierra el hueco que Cargo_Architecture.md §13 tiene anotado como
    "compat con mods externos de NVG/ópticas: falta mapear mods concretos".
    RUTA DE SLOT, DECIDIDA POR EL AUTOR: las DOS. Con casco, las gafas montan
    en su sub-slot óptica (§4, ya existe y cargo_dev_helmet ya lo declara);
    sin casco ocupan Head directo — cuesta UNA cadena, el filtro del slot head
    pasa a "category:helmets,optics" y MatchesFilter ya parsea listas. Ni un
    primitivo nuevo: CRG-8 intacto. La regla que percibe el jugador es "o el
    casco con las gafas montadas, o las gafas solas".
    CUATRO HUECOS, y son de Cargo, no del mod:
    (a) NO HAY SEÑAL DE EQUIPAR. Equip/Unequip solo dan/quitan weapon_class y
        la única difusión es Corpus_Cargo_BodyChanged, exclusiva del slot body;
        SubSlotAttach/Detach no difunden nada. Hace falta un
        Corpus_Cargo_EquipChanged genérico (+ Corpus_Cargo_SubSlotChanged),
        SUMADO al BodyChanged, que se queda como está (Cortex cuelga de él).
        Cargo difunde "cambió el slot X"; la semántica vive en el compat —
        CRG-1 intacto. Descartado def.onEquip: 61 defs cargando la misma
        closure es más superficie por el mismo resultado.
    (b) LA ENTIDAD NO-ARMA ESQUIVA EL PORTERO. El PlayerUse de capture.lua
        atiende tres formas y sale en "if not ent:IsWeapon() then return end":
        hoy E sobre unas gafas llega al ENT:Use del mod y el inventario nunca
        se entera. La técnica de bloqueo ya está probada (return false, #27);
        exponerlo como Capture.RegisterWorldPickup(class, spec) — patrón de
        StatusPanel.RegisterBar — en vez de una cuarta rama inline.
    (c) El slot Head no acepta ópticas sueltas (ver ruta arriba).
    (d) Nada más: model/icon/has_condition/size/value/equip_slots ya existen.
    EL COMMIT ES ESCRIBIR LA NW, NUNCA ArcticNVGs_SetPlayerGoggles: esa función
    DROPEA al mundo el par anterior, o sea que duplica un ítem del que Cargo ya
    es dueño. ply:SetNWInt("nvg", id) directo, validando el nombre corto antes
    (el mod no valida: nombre inexistente -> SetNWInt con nil -> error).
    EL ID DEL MOD ES UN ORDINAL de su tabla, no un nombre: se persiste el
    ShortName y el ordinal se resuelve al equipar. Mismo espíritu que CRG-42.
    LOS DEFS SE DERIVAN, NO SE CATALOGAN (lección de CRG-41/42): 61 variantes
    sobre 6 modelos; se itera la tabla global ArcticNVGs (shared, existe en
    ambos realms), el PrintName sale de la entidad y el ícono del PNG que el
    propio mod trae por variante (def.icon gana en el pipeline y descarta
    materiales rotos solo). A mano, SOLO la tabla de 6 familias: peso, valor y
    footprint. Convar cargo_nvg_register para no inflar el catálogo del trader.
    sh_arctic_nvg_losegoggles VA A 0: Cargo es dueño de qué se pierde al morir
    (cargo_lose_on_death) y su reconcile de PlayerLoadout re-escribe la NW. El
    orden de hooks favorece al reconcile, pero depender del orden es el bug que
    Quick Loadouts ya costó — se apaga el convar y se avisa UNA vez por
    Corpus.Log si se lo encuentra en 1; jamás pisarlo en silencio.
    Mapeo completo, API verificada contra el código vivo y defectos del mod
    inventariados en dev/Cargo_NVG_Neosun_Referencia.md. El mod es OFF-LIMITS
    para modificar (dato del autor): integración en runtime y nada más.

48. LA CELDA ANCHA Y EL CLICK EN EL WHEEL [Cargo UI] — CERRADO 2026-07-29,
    entries 54-55 [APLICADO], planilla W en 10/10 (dos rondas). (Pedido del
    autor 2026-07-29, al cerrar el #46 con la planilla V en 10/10.)
    PROMPT: ../../dev/PROMPT_cargo_wheel_celda_ancha_y_click.txt
    LO QUE LA TANDA DEJA COMO METODO, y es lo unico que vale reescribir: sus
    dos lecciones son sobre INSTRUMENTOS, no sobre el motor. (1) La unica
    ronda que se perdio fue la medicion, y por el instrumento: la primera
    sonda contaba los clicks pero NO DIBUJABA NADA. (2) El hallazgo de la
    tanda —un check que pasaba con la implementacion buena Y con la mala—
    salio de REVERTIR EL ARREGLO, no de la corrida verde. Tercera vez en este
    bloque, asi que la consigna se endurece: LA REVERSION ES EL UNICO
    INSTRUMENTO QUE AUDITA AL INSTRUMENTO.
    El hueco del paso 1 lo cerro el autor: la resolucion es 16:9, o sea que la
    cuenta del desborde esta MEDIDA en el caso real y no solo calculada. El
    4:3 sigue declarado como caso apretado y SIN MEDIR — frontera conocida.
    (A) LA CELDA ANCHA — entry 54
    [APLICADO], planilla W 6/6 sobre el paso 1, UNA sola ronda y sin
    defectos. Harness 669 (eran 639), 30 checks nuevos y 5 reversiones
    verificadas en negativo. La cuenta del desborde quedo confirmada DOS
    veces: por el harness contra el layout real —GroupOuterExtent lee la tabla
    REF que usa BuildLayout, asi que 501/581/661 @1080 son los del layout, no
    numeros que el check se pase a si mismo— y por W4 en juego, donde la
    columna con empuje no se salio de pantalla. El mock NO se edito.
    LO QUE LA RONDA NO DEJO: los seis pasaron SIN NOTA, asi que la regla del
    #46 (pedir la nota aunque el check pase) no se ejercito. El hueco concreto
    es la RESOLUCION DE PANTALLA que pedia W4: la cuenta esta verificada para
    16:9 y el caso apretado declarado es 4:3, sin medir.
    (B) EL CLICK — entry 55 [APLICADO], confirmado en W8/W9/W10.
    LA INCOGNITA SE MIDIO ANTES DE ESCRIBIR UNA LINEA (W1, en juego) y salio
    que SI: el screen clicker se traga el click como input de JUEGO pero no
    impide leer el ESTADO del boton por debajo — misma maquinaria que el menu
    contextual del propio GMod. Si hubiera salido que no, esta entry no
    existiria.
    La medicion costo una ronda por el INSTRUMENTO y no por el motor: la
    primera sonda que llego a juego era el fragmento abreviado copiado a mano,
    que contaba los clicks pero NO DIBUJABA NADA. Misma familia que este
    bloque viene nombrando desde el #46, solo que el que no distinguia "roto"
    de "no paso nada" era el instrumento. La definitiva mide CON BASELINE
    (columnas CON y SIN el wheel abierto).
    Forma: convar cargo_wheel_click (default 1: SUMA un gesto y no cambia
    ninguno), commit por Close(true) -> Commit, polleo con flanco en el MISMO
    Think de la tecla. NO ESTRENA API: input.IsButtonDown es la que ese hook
    ya llamaba por frame, preguntando por MOUSE_LEFT en vez de un KEY_.
    La trampa (un solo commit por apertura) NO necesito codigo: Close sale por
    state nil. Necesito probarse en negativo, y se probo.
    EL FLANCO SE OBSERVA SIEMPRE, abierto o cerrado — si no, abrir el wheel
    con el boton de disparo apretado comitearia en el acto.
    Y UN CHECK QUE NO MEDIA LO QUE DECIA, destapado por la reversion y no por
    la corrida verde: el bloque del click corre ANTES que el de la tecla, asi
    que en el frame de apertura el state todavia es nil y cualquier version
    pasaba; el defecto vivia un frame mas tarde. Corregido con un Think de
    mas. Tercera vez en este bloque que un check nace sin distinguir, y las
    tres las destapo revertir el arreglo.
    Harness 676 (eran 669), 7 nuevos y 4 reversiones en negativo.
    NO ENTRA: el boton derecho / accion alternativa (ToggleStat(addr, -1) de
    ARC9) — bloque propio.
    DOS COSAS, y la primera NO es diseño nuevo: es la DIFERIDA (g) del #46,
    aprobada por el autor el 2026-07-27 con el mock como sede
    (mockups/cargo_wheel_lights_mock_v1_1.html, bloque 06).

    (A) CELDA ANCHA — variante (B) del mock, 150x56, SOLO para el grupo de
        luces. Configurable por convar. Las dos condiciones duras son del
        mock y no se negocian:
          * SOLO con anclaje izquierda o derecha. Con anclaje arriba/abajo el
            grupo DEGRADA SOLO a 56x56 en el MISMO build — las dos anatomias
            de celda coexisten, no es un modo que se elige una vez.
          * QUICK Y TOOLS SIGUEN CUADRADOS SIEMPRE. La celda ancha es la
            excepcion del panel de luces, no un modo nuevo del menu.
        Impacto real, ya localizado: GroupCells (client/corpus_cargo_wheel.lua)
        cierra hoy sobre UNA constante de celda; pasa a (w,h) por grupo, con w
        clampeado a h cuando el eje es horizontal. ResolveAnchors no se toca.
        LA ADVERTENCIA DEL MOCK SOBRE EL DESBORDE ESTA MAL Y SE CORRIGE CON LA
        CUENTA, no con una opinion: el mock dice que con empuje la columna
        "se sale de pantalla en 1280x720". El grupo crece hacia AFUERA desde la
        misma linea de anclaje, y el extremo queda en 305 (anillo) + 46
        (margen) + push*(56+24) + 150 (celda) = 501 con push 0 y 581 con push 1,
        en unidades @1080 que el layout ESCALA por altura. A 1280x720 son ~334 y
        ~387 px reales contra 640 de media pantalla; el caso apretado no es
        16:9 sino 4:3, y el push=2 que dispara el peor numero (661) es
        INALCANZABLE por convars — ResolveAnchors nunca deja a quick y tools en
        el mismo lado. Se fija con un check de harness sobre la pura, para que
        nadie re-herede la advertencia sin recalcularla.

    (B) CLICK EN EL WHEEL — ADITIVO, decision del autor: soltar la tecla sobre
        algo sigue comiteando EXACTAMENTE como hoy (CRG-31 intacta) y ademas,
        con el wheel abierto, un click comitea. No se reemplaza el gesto que ya
        esta en el musculo; se suma el pausado.
        DOS COSAS QUE NO SE ASUMEN, y es la leccion que el #46 pago TRES veces
        (una API del engine asumida en vez de medida):
          * COMO se detecta el click. NO se estrena un hook de mouse de GMod de
            memoria: no hay dev/other/ contra el cual verificarlo. Se pollea
            con deteccion de flanco, que es el patron que el wheel YA usa para
            su tecla (input.IsButtonDown en Think) — precedente dentro del
            propio archivo en vez de API nueva.
          * SI el screen clicker deja ver el estado del mouse por debajo. Hoy
            gui.EnableScreenClicker se traga los clicks A PROPOSITO (§17.4:
            nada dispara mientras apuntas el wheel). Que input.IsMouseDown siga
            reportando es PLAUSIBLE y NO ESTA VERIFICADO. Es lo PRIMERO que
            mide la planilla, no algo que se descubre a mitad de la
            implementacion; si no reporta, el plan B se decide con el autor.

    NO ENTRA EN ESTA TANDA: las otras dos diferidas del #46 — (h) fades, ciclo
    de cierre y estado BLOQUEADO de rechazo visible, e (i) baterias 0-100.
    Siguen aprobadas y diferidas, con el mock como sede. DE (h) SE ACTUALIZA
    UNA COSA: arrastraba una verificacion pendiente —si el mod de NVG bloquea
    la linterna durante su animacion— y la planilla V la CONTESTO en V7: NO la
    bloquea, la linterna encendio en pleno transito. Si (h) se ejecuta algun
    dia, arranca sin esa incognita.

49. EL CLICK QUE NO CIERRA: CICLAR SIN REABRIR EL WHEEL [Cargo UI] — CERRADA
    el 2026-07-30. Entry 56 [APLICADO 2026-07-29]; planilla X cerrada. La
    cabecera decia "[PENDIENTE], esperando la planilla" hasta el 2026-08-20:
    drift de tres entradas que el cierre del arco #48-#52 no bajo al rumbo.
    (Pedido del autor 2026-07-29, al cerrar el #48 con la planilla W en 10/10.)
    LAS DOS DECISIONES SE CONSULTARON ANTES DE ESCRIBIR y las dos las tomo el
    autor: (a) el modo es SOLO para los chips de luz; (b) si hubo un click
    sostenido en esa apertura, soltar la tecla SOLO cierra. Con (b) CRG-31
    queda LITERAL en el camino default —sin clicks, soltar comitea— y la
    excepcion solo existe tras un gesto que el jugador pidio.
    Convar cargo_wheel_click_sticky, default 0: el default del #48 no cambia.
    La politica del soltar vive en CloseOnRelease y NO dentro de Close, que
    sigue siendo un "comitea o no, vos decidis" tonto (el camino del click
    necesita la respuesta contraria); la consumen el poll de Think Y el
    concommand -cargo_wheel, que asi no se desincronizan.
    UN CHECK QUE NO DISTINGUIA, OTRA VEZ: el del alcance pasaba tambien con
    una version donde el sticky se derramaba a TODOS los objetivos, porque
    contaba commits y el conteo es identico — lo que cambia es si el menu
    quedo abierto. Se arreglo con un SEGUNDO click, que sobre un wheel cerrado
    no comitea nada. Cuarta vez en el arco, y dos seguidas destapadas por la
    reversion y no por la corrida verde.
    Y EL HARNESS DEJO DE MENTIR SU PROPIO TOTAL: perseguir un desajuste de +-1
    destapo que el numero que los docs citan salia de GREPEAR "[ok]" en
    stdout, donde el banner de realm de Python corre carreras con los print de
    Lua y llegaba a tragarse una linea entera. Ahora cada pasada cuenta
    (CHECKS_OK) y el script IMPRIME su total. El valor no cambio (683, y
    639+44 cuadra exacto): cambio su PROCEDENCIA.
    Harness 683 (eran 676), 7 nuevos y 3 reversiones en negativo.
    TEXTUAL: "me gustaria una opcion para que el clic no cierre el wheelmenu,
    sino para que uno apriete sin cerrar, asi puedo ciclar el attachment de
    arc9 en una sola pasada sin mover tanto el mouse".
    ES LA CONTINUACION NATURAL DEL #48(B) y NO estaba en su alcance: aquella
    tanda declaro explicitamente que el click comitea y CIERRA, y eso se
    confirmo en W8. Esto es un modo nuevo, con convar propia.
    POR QUE TIENE SENTIDO, en el vocabulario que el propio wheel ya usa: los
    sectores EQUIPAN, los quick USAN, las luces TOGGLEAN (§17.6/§17.8). De los
    tres verbos, TOGGLEAR es el unico REPETIBLE — ciclar el modo de un
    dispositivo ARC9 es literalmente apretar N veces. Equipar dos veces la
    misma arma no significa nada (y por el re-press de #22 significa
    enfundar); usar dos veces un quick GASTA DOS ITEMS, que es el riesgo real
    del modo.
    LA DECISION ABIERTA, Y ES DEL AUTOR (§7.5 — no se elige sola):
      (a) ALCANCE. Sticky para TODO lo que el wheel comitea, o sticky SOLO
          para los chips de luz (el unico verbo repetible), dejando que el
          click en sector/quick/tool siga comiteando y cerrando como hoy.
      (b) QUE HACE SOLTAR LA TECLA DESPUES DE HABER CLICKEADO. Es la pregunta
          cara, porque CRG-31 dice que soltar comitea: si se respeta al pie de
          la letra, ciclar 3 veces con clicks y soltar sobre el mismo chip
          dispara un CUARTO toggle que nadie pidio. Las salidas son suprimir
          el commit de soltar cuando ya hubo un click en esa apertura (CRG-31
          intacta en el camino default: sin clicks, soltar comitea igual que
          siempre), o exigir mover el cursor a la deadzone antes de soltar
          —que es la regla de cancelacion que YA existe— a costa de pedir
          justo el movimiento de mouse que el pedido quiere evitar.
    NO SE TOCA EL DEFAULT: cargo_wheel_click 1 sigue comiteando y cerrando.
    Lo de acá es una convar mas, apagada por default.
    HERMANO ANOTADO: el BOTON DERECHO sigue siendo bloque propio (ciclar el
    modo HACIA ATRAS con ToggleStat(addr, -1), que ARC9 soporta y ya esta
    anotado en el header de client/corpus_cargo_lights.lua). Si el modo
    sticky entra, el derecho se vuelve mas util todavia — pero son dos
    bloques, no uno.

50. EL BOTON DERECHO CICLA EN REVERSA [Cargo UI] — CERRADA el 2026-07-30.
    Entry 57 [APLICADO 2026-07-29]; planilla Y cerrada. Cabecera corregida el
    2026-08-20 (mismo drift que la 49 y la 51).
    Pedido del autor EN LA NOTA DE X1, un check que PASO — quinta vez en el
    arco que el hallazgo sale del campo de notas y no de un rojo: "sobre los
    ARC9 lo unico que pediria seria que con el clic secundario del raton se
    pudiera ciclar en reversa (No se si ARC9 lo permite, tendrias que
    investigar)".
    LA INVESTIGACION QUE PIDIO, CONTRA dev/other/ Y NO DE MEMORIA (CRG-24):
    SWEP:ToggleStat(addr, val) — sh_attach.lua:718-735. val = val or 1, suma
    el paso a ToggleNum, y EL WRAP ESTA ESCRITO EN LAS DOS DIRECCIONES
    (> #ToggleStats -> 1, y < 1 -> #ToggleStats). ARC9 ANTICIPO el paso
    negativo. Dos cosas mas que salieron de leerla: NO llama a PostModify (el
    commit lo paga, igual que el de ida) y un dispositivo de UN solo modo
    envuelve sobre si mismo en ambos sentidos — no-op honesto, jamas error.
    FORMA: el registro de fuentes gana toggleBack(ply, wep) OPCIONAL, y su
    ausencia es la respuesta honesta — una linterna no tiene reversa, y nada
    mas del wheel la tiene (desequipar no es "equipar hacia atras"). El
    derecho se pollea en el MISMO Think y por el MISMO camino que el
    izquierdo (ClickCommit(back)): un solo codigo, no una copia que derive.
    Hereda gratis el modo del #49 y la convar cargo_wheel_click.
    TRES DEFECTOS DE CHECK, LOS TRES POR LA REVERSION:
      (1) el and/or que cae hacia adelante. Escribirlo largo parecia
          redundante y NO lo era: el pick corre DOS veces (una para decidir,
          otra dentro de Close) y el cursor puede moverse entre ambas. El
          check ahora mueve el cursor entre pick y pick a proposito.
      (2) el guard de ausencia media menos de lo que decia: quitarlo no hacia
          caer nada, porque el no-op "no hace nada" igual — pero SI hace algo
          invisible, marca que hubo click y entonces soltar deja de comitear.
          Un boton sin efecto se comia el gesto de siempre.
      (3) el de siempre: un check que solo cuenta commits no ve si el menu
          quedo abierto.
    Harness 694 (eran 683), 11 nuevos —tres sobre el dispositivo ARC9 REAL
    del bloque del #46, no sobre una fuente sintetica— y 4 reversiones en
    negativo.

51. LA LINTERNA TAPADA POR EL DISPOSITIVO DEL ARMA [Cargo UI] — CERRADA el
    2026-07-30. Entry 58 [APLICADO 2026-07-29]; planilla Z cerrada. Cabecera
    corregida el 2026-08-20 (mismo drift que la 49 y la 50).
    Pedido del autor EN LA NOTA DE Y3, un check que PASO — SEXTA vez en el
    arco: "al tener el arma de ARC9 con dispositivo se bloquee visualmente la
    linterna con tachas, mejor que rojo se puede copiar las tachas que tienen
    los otros bloques del wheelmenu asi como el quickslot, pues un jugador
    puede pensar que la linterna del engine esta disponible, cuando en
    realidad la de ARC9 la captura".
    EL DIAGNOSTICO CORRIGE UNA AFIRMACION DE NUESTRO PROPIO DOC. §13 y la
    entry 53 decian que el haz no se dibuja por "supresion de render de ARC9".
    EL CODIGO NO DICE ESO: grepeada la base entera, ARC9 toca la linterna del
    ENGINE en UN SOLO lugar (sh_attach.lua:143-144, PostModify la APAGA si el
    arma tiene un att ToggleOnF) y NO hay ninguna ruta de supresion de render
    para ella — todos los render.SuppressEngineLighting son de VGUI, FLIR y
    presets, o sea previews. Su cl_light.lua administra SUS PROPIAS projected
    textures y nada mas. El mecanismo real NO ESTA IDENTIFICADO; la hipotesis
    viva es el presupuesto de projected textures del engine, y queda anotada
    COMO HIPOTESIS. Era una inferencia redactada como medicion — el mismo
    error que el UnequipDelay del #46.
    FORMA: state() puede devolver blocked = "<motivo>". El chip se pinta con
    el tramado de 45° de los quickslots bloqueados, en border y NO en rojo
    (decision del autor: el rojo lee como error y esto no lo es), scissoreado
    por CRG-28 y valido en las DOS anatomias. En el hub cambia el HINT, no el
    estado. El predicado es el de ARC9 y no uno nuestro: ToggleOnF, leido de
    la misma lista de slots que los chips de dispositivo ya recorren.
    LA DECISION DE DISENO: `on` NO se falsea a false. La linterna ESTA
    encendida; decir OFF seria la mentira de CRG-32 y esconderia el estado
    real al cambiar de arma. "Encendida pero no la vas a ver" es un TERCER
    hecho y vive en su propio campo — ESTADO Y DISPONIBILIDAD SON PREGUNTAS
    DISTINTAS, y confundirlas es lo que hacia al chip cierto e inutil a la vez.
    El toggle SIGUE funcionando: la accion no esta rechazada.
    Harness 698 (eran 694), 4 nuevos y 2 reversiones en negativo, con la
    ausencia por partida doble (sin arma, y con un ARC9 sin dispositivo).

52. EL COMMIT SOBRE UNA FUENTE TAPADA SE RECHAZA [Cargo UI] — CERRADO
    2026-07-30, entry 59 [APLICADO], planilla AA en 3/3 (una ronda).
    CIERRA EL ARCO #48-#52 ENTERO. Y lo que mas dice esta seccion es lo que NO
    trajo: las siete anteriores terminaron con una nota que abria el bloque
    siguiente —X salio de W, Y de X, Z de Y, AA de Z— y esta no abrio ninguno.
    La cadena se detuvo sola, y no por dejar de mirar: AA3 recorrio a
    proposito todo lo de las cinco tandas y volvio sin nada.
    EL ARCO EN NUMEROS: entries 54-59, dos dias, 23 checks de planilla en
    cinco secciones y NI UNA RONDA PERDIDA POR UN DEFECTO DE CODIGO — la unica
    que costo una vuelta fue la medicion de W1, y por el INSTRUMENTO. Harness
    639 -> 700: 61 checks nuevos y 19 REVERSIONES VERIFICADAS EN NEGATIVO. Ni
    una norma nueva, ni un mensaje de red, ni una linea de server en las cinco.
    LAS TRES REGLAS DE METODO QUE DEJA, y las tres se pagaron:
      (1) LA REVERSION ES EL UNICO INSTRUMENTO QUE AUDITA AL INSTRUMENTO.
          Siete checks nacieron sin distinguir y NINGUNO lo destapo la corrida
          en verde: los destapo revertir el arreglo y mirar si salia el rojo.
      (2) UNA INFERENCIA NO SE ESCRIBE COMO UNA MEDICION. Encontrado dos veces
          en nuestros propios docs: el UnequipDelay del #46 y la "supresion de
          render de ARC9" de §13.
      (3) UN ARGUMENTO SOBRE EL INSTANTE PUEDE SER FALSO SOBRE LA SECUENCIA
          (la trajo el autor corrigiendo la entry 58): una accion puede ser
          legal ahora y morir en el gesto siguiente.
    Pedido del autor EN LA NOTA DE Z3, un check que PASO — SEPTIMA vez en el
    arco. CORRIGE LA DECISION DE DISENO DE LA ENTRY 58, y hay que decir por
    que: aquella declaro que el toggle seguia funcionando porque "bloquear el
    commit seria inventar un rechazo que el mod no impone". ES FALSO, y el
    autor tenia el dato: el mod SI lo impone, en diferido — sh_attach.lua:
    143-144 apaga la linterna del engine EN CUANTO se cicla el dispositivo.
    Encenderla bajo ese arma no es una accion valida que estuvieramos
    estorbando: es una que MUERE EN EL SIGUIENTE CLICK. El razonamiento viejo
    miraba el INSTANTE; el del autor mira la SECUENCIA.
    FORMA: una fuente que se declara blocked rechaza el commit en las dos
    direcciones y por las dos rutas. UNA regla (BlockedChip) con DOS
    consumidores: Commit rechaza, y ClickCommit lee la misma respuesta para
    decidir que UN RECHAZO NO CIERRA EL MENU — cerrar es como el wheel acusa
    recibo de un commit, y no hubo ninguno. Mas el DESTELLO: un pulso rojo de
    250 ms sobre la celda, pintado AL FINAL (sobre el tramado y las barras).
    NO es el bloque 07 del mock (dos parpadeos + fade + cue), que sigue
    diferido porque arrastra el dibujador raiz. Los dos canales dicen cosas
    distintas: el tramado es "esto no esta disponible", el destello es "y
    acabo de rechazar lo que apretaste".
    EL DESTELLO SE RESUELVE EN EL PINTOR Y NO SE GUARDA EN EL CHIP: la lista
    se re-arma al cambiar de arma (§17.8, V8), asi que colgarlo de la tabla
    del chip lo evaporaria a mitad de ventana — mismo motivo por el que el
    transito del NVG vive en un upvalue del archivo de luces.
    FRONTERA DECLARADA: al SOLTAR la tecla sobre una tapada el rechazo ocurre
    igual pero el wheel cierra, asi que el destello no llega a verse. El
    tramado ya aviso; el destello es para el gesto que puede quedarse mirando.
    Harness 700 (eran 698), 2 nuevos y 1 reversion en negativo. El segundo
    check es el que hace distinguir al primero: destapada, el MISMO gesto
    sobre el MISMO chip SI comitea.

53. LA CONFIGURACION ARC9 NO SOBREVIVE A NADA [Cargo arc9/inventory, bug] —
    CERRADO 2026-07-31, entries 61-64 [APLICADO], planilla AB en tres rondas
    (12 en PASA; AB8 retirado por decision, AB14 sin correr). Harness 700 ->
    771, 15 reversiones verificadas en negativo.
    LO QUE DECIDIO EL BLOQUE FUE UNA MEDICION Y NO UNA LECTURA: AB1 midio que
    wep.Attachments es POR ENTIDAD (dos AS VAL mod4 vivas con Installed
    distinto en el mismo slot), asi que el invariante pedido era alcanzable y
    el plan B quedo descartado MEDIDO. Su receta nacio corregida: un jugador
    sostiene UN SWEP por clase, asi que las dos armas no pueden estar las dos
    en el inventario — tienen que ser dos entidades vivas, una en la mano y
    otra en el suelo.
    DOS DECISIONES DEL AUTOR CAMBIARON EL DISENO, Y LAS DOS POR UN CASO REAL:
    (1) "si el objeto no puede entrar al inventario, debe caer al piso" ->
    CRG-65, y el mismo modo de falla aparecio en un SEGUNDO sitio que no era
    ARC9 (el espejo del pool de municion, con un comentario que decia literal
    "The rounds are NOT destroyed" desde el dia que se escribio). (2) El peso
    de los atts se DIFIERE al #55: su MCX de 2,9 kg lleva DOCE attachments, y
    con 0,3 kg planos las piezas pesarian mas que el arma — cuando un numero no
    sobrevive el contacto con un caso real, el defecto suele estar en el modelo
    y no en el numero.
    LA FRONTERA QUE QUEDA DECLARADA: la ruta "montar un att desde el menu C" no
    se ejercio CON NUMEROS en ninguna de las tres rondas. AB13 respondio su
    pregunta por la NEGATIVA (botados los repuestos, grid=0 y hook=0 en las
    siete lineas y el menu dejo de ofrecerlos: el puente contesta lo que el
    grid tiene y no hay un tercer inventario), pero esa misma accion destruyo
    la precondicion de la resta y con ella AB14. La entry 64 se sostiene en sus
    6 checks de harness con el hook realmente cableado. Ver CHANGELOG 64.
    -- el pedido original, para referencia --
    Pedido del autor 2026-07-30, con el sintoma
    y la sospecha ya separados: "suelo perder las linternas como attachments de
    mis armas ARC9", y la pregunta partida en dos — sobreviven a la
    persistencia cuando ya estan en el arma? sobreviven a pasar del equipo al
    inventario?
    LA RESPUESTA A LAS DOS ES NO, Y LA PRIMERA ES AL REVES DE LA INTUICION.
    DIAGNOSTICO POR LECTURA DE CODIGO, NO POR MEDICION — se dice asi a
    proposito: es la regla que dejo el arco #48-#52 (una inferencia no se
    escribe como una medicion) aplicada al doc que abre el bloque. El
    instrumento para medirlo es la planilla AB del handoff.

    DONDE VIVE EL ESTADO MONTADO: en un solo lugar, la tabla Lua de la entidad
    SWEP (slottbl.Installed, sh_attach.lua:19). Nada en el jugador, nada en
    disco del lado server. Y el blob de instancia de Cargo NO TIENE CAMPO DE
    ATTACHMENTS (instances.lua:45-59 — id, condition, zones, subslots,
    ammo_group; mas clip1, que se cosecha a mano en StoreClip). El cargador
    tiene ruta de guardado; el arbol de atts no tiene ninguna. De paso: dos
    comentarios nuestros afirman lo contrario (capture.lua:641 y :653, "el blob
    restaura la condicion, los attachments y el cargador") y la fila del
    savegame de §13 los repite. Hay que corregirlos en la misma tanda.

    LAS CINCO PUERTAS SON UNA SOLA COSA: la entidad SWEP se destruye con atts
    adentro, y ARC9 no devuelve nada en OnRemove/OnDrop (sh_init.lua:215-234).
      (1) equipo -> inventario: Unequip -> StripEquipWeapon -> StripWeapon.
      (2) drop SIN el arma en la mano: StripWeapon + SpawnWorldWeapon, que crea
          una entidad NUEVA y solo re-aplica el cargador.
      (3) WALK+USE de un arma del suelo: la captura hace wep:Remove()
          (capture.lua:1136) para convertirla en item del grid.
      (4) respawn / reconexion / cambio de mapa: RegiveEquipped da la clase de
          nuevo, entidad nueva y desnuda.
      (5) muerte con cargo_lose_on_death — esa si es intencional.
    La unica transicion que SI los conserva es el drop con el arma EN LA MANO:
    ply:DropWeapon deja la MISMA entidad en el suelo, con su arbol y
    dibujandose. Hasta que la levantas, y ahi cae en la puerta 3.

    POR QUE PARECE QUE SOBREVIVEN, Y POR QUE TE COME REPUESTOS: ARC9 tiene un
    autoguardado propio, DEL CLIENTE y POR CLASE de arma (arc9_autosave, default
    1) que re-aplica el preset 0,075 s despues del primer Think de cada entidad
    nueva (sh_think.lua:208-223). Eso manda el arbol al server, que lo valida
    contra el almacen — que somos nosotros — en ValidateInventoryForNewTree
    (sh_subatts.lua:242-263). Dos desenlaces, y ninguno es continuidad:
      - CON otra linterna en la mochila: la validacion pasa y el diff de
        sh_net.lua:90-122 llama PlayerTakeAtt, o sea que nuestro hook la
        DESCUENTA del grid. El arma "volvio con la linterna" y desaparecio un
        item del bolso. Cada ciclo se come uno.
      - SIN repuesto: la validacion falla (la instalada se destruyo y el
        contador de Cargo es 0), el server re-manda su arbol vacio y el arma
        vuelve pelada.
    Lo que hoy parece persistencia es un preset POR CLASE pagado con items del
    inventario. Dos armas iguales comparten esa configuracion.

    SEGUNDA FUGA, INDEPENDIENTE Y MAS BARATA DE ARREGLAR: desacoplar con la
    mochila llena DESTRUYE el attachment. El hook ARC9_PlayerGiveAtt
    (arc9.lua:117-124) llama GiveItem, IGNORA su valor de retorno y devuelve
    true igual — y GiveItem rechaza por peso (inventory.lua:380, "too heavy").
    ARC9 da el att por entregado y nadie lo tiene. Es la forma exacta de error
    que CRG-9 existe para prohibir, en los slots de un tercero.

    LO QUE EL AUTOR DECIDIO (2026-07-30, textual): "lo que quiero es conservar
    la configuracion de ARC9 en el equipamiento del arma y que el cambio de
    attachments vuelva al inventario, es decir, si quito la linterna, esta debe
    volver al inventario como un item mas. Si boto un arma ARC9 con mira
    telescopica, toda esa configuracion va con el drop."
    Son TRES invariantes, y el segundo ya existe a medias:
      (a) un att instalado PERTENECE A LA INSTANCIA DEL ARMA, no al jugador:
          viaja con ella al grid, al suelo, al contenedor y al savegame.
      (b) sacarlo devuelve un item al inventario — ESTO YA FUNCIONA por el diff
          de ReceiveWeapon; lo que falta es que no se pierda cuando el grid no
          tiene lugar (la segunda fuga).
      (c) nada se destruye como efecto colateral: CRG-9 aplicado a los slots de
          un tercero.

    FORMA PROPUESTA (nada escrito todavia; se discute al abrir el bloque):
      - blob.atts: nuestro propio arbol PLANO — solo strings y enteros
        (shortname del att, ToggleNum, y la categoria del slot). JAMAS las
        tablas de slot de ARC9: llevan Material() y referencias ciclicas
        adentro, que es exactamente lo que rompe gm_save (§13) y lo que el
        guard offline rechaza.
      - se cosecha COMO clip1: en la puerta, antes de que la entidad muera, no
        despues. StoreClip es el precedente exacto y el molde a copiar.
      - se re-aplica por la ruta del propio mod y del lado server:
        BuildSubAttachments + PruneAttachments + SendWeapon + PostModify, que es
        literalmente el bloque server de ReceiveWeapon (sh_net.lua:141-149), y
        todo eso es realm SHARED (el autoinclude de shared.lua:1530-1557 corre
        los sh_ en los dos). Cero API nueva: CRG-23 en pie, la escritura va por
        la API del mod.
      - el autoguardado del cliente hay que APAGARLO para nuestras armas o
        pelea con el blob: SetNoPresets(true) es NetworkVar del propio ARC9
        (shared.lua:1632) y es lo que el mod usa para las armas de sus NPC
        (sv_npc.lua:226). Con eso el blob queda como fuente unica.
      - ORDEN CONTRA EL CARGADOR: el PostModify de server, si el ClipSize
        cambio —y un cargador extendido lo cambia—, hace Unload +
        SetRequestReload (sh_attach.lua:147-159). El arbol va ANTES de
        RestoreClip o la restauracion se vacia al pool. Es un argumento sobre la
        SECUENCIA: se mide, no se supone.
      - el indice de slot de ARC9 es un ORDINAL DE UN TERCERO (CRG-63): un pack
        que inserte un slot corre los indices y el arbol guardado montaria la
        mira en otra parte. Se persiste el shortname (clave estable, como el
        ShortName de #47) y la categoria del slot, se VALIDA antes de escribir,
        y si no calza el att vuelve al inventario en vez de perderse.

    LA PRECONDICION QUE DECIDIA EL BLOQUE — MEDIDA EL 2026-07-30, check AB1:
    wep.Attachments es POR ENTIDAD. Dos AS VAL mod4 vivas a la vez con
    Installed DISTINTO en el mismo slot (una con eft_tactical_peq2 en el 11 y
    la otra sin el) y las tres tablas en direcciones distintas. El invariante
    (a) es alcanzable y el plan B quedo descartado con medicion. El instrumento
    es cargo_dev_attshare, y nacio corrigiendo la receta: un jugador sostiene
    UN SWEP por clase, asi que las dos armas no pueden estar las dos en el
    inventario — tienen que ser dos ENTIDADES vivas, una en la mano y otra en
    el suelo. Texto original de la pregunta: si wep.Attachments
    es por ENTIDAD o compartida por CLASE. ARC9 nunca copia esa tabla al
    inicializar (solo DefaultAttachments, sh_init.lua:43) y escribe Installed
    ATRAVES de ella, asi que si el engine no entrega una copia profunda por
    entidad, dos armas de la misma clase comparten configuracion y el
    invariante (a) es INALCANZABLE tal como esta pedido. Se mide con dos armas
    iguales y una linea de consola, y va PRIMERO en la planilla: el resto del
    bloque depende de la respuesta.

    DECISIONES ABIERTAS PARA EL AUTOR: que pasa con los atts montados cuando el
    arma se VENDE (hoy el precio no los cuenta) y cuando se destruye con
    cargo_lose_on_death; y si un att montado PESA (hoy el peso del arma no
    incluye lo instalado, y el nominal de attachment es 0,3 kg).

    ENLAZA: el #42 (lanzagranadas capturado sin su att de municion) es la misma
    raiz por el otro lado — FillIntegralSlots solo llena si el jugador POSEE el
    att, y con Cargo como almacen no lo posee. Y la frontera ya declarada de
    §13 (un arma ARC9 en el suelo rompe gm_save por sus IMaterial) es la razon
    de que blob.atts tenga que ser plano y nuestro.
    Sede de lo ya decidido, ahora que el #53 cerro: Cargo_Architecture.md §5
    (CRG-66 y su enmienda) + §10.5 (la forma del arbol), CHANGELOG 63 y 64. El
    handoff de dev/ ya cumplio y no se cita — dev/ no esta versionado.

54. EL ICONO NO DISTINGUE DOS ARMAS DE LA MISMA CLASE [Cargo icons, UX] —
    ABIERTO, SIN DISENAR. Pedido del autor 2026-07-31, salido de la NOTA del
    check AB9 de la planilla, que estaba en PASA: "el icono autogenerado se
    genera para las dos armas, incluso una que tenga mira y la otra no".
    Con el #53 cerrado dos instancias de la MISMA clase ya no son
    intercambiables —una lleva mira telescopica y la otra no, y desde ahora eso
    pesa distinto y vale distinto—, pero en el grid se ven identicas: el
    pipeline de iconos rinde por DEF (clase), no por instancia.
    FORMA QUE PROPUSO EL AUTOR, textual: "en la autogeneracion puede existir el
    modelo base del arma (El arma sin attachments) y las armas que sean
    diferentes en attachments se vuelvan a autogenerar en una nueva imagen".
    O sea: el render por def sigue siendo el default barato, y una instancia
    cuyo arbol difiera del build de fabrica acuna su propio render.
    LO QUE HAY QUE DECIDIR ANTES DE ESCRIBIR: donde vive ese render (el cache
    de PNG es POR CLIENTE y no persiste namespaced — Cargo_ItemImages §4.3), y
    cual es la clave — el arbol entero es demasiado fino (cambiar de modo de un
    dispositivo no deberia acunar un icono nuevo) y la clase es demasiado
    grueso. Enlaza con la deuda ya declarada de la fuente ARC9 a 256^2.
    Semilla: la nota de AB9 en la planilla.

55. QUE ATTACHMENTS PESAN [Cargo weight, diseno] — ABIERTO, SIN DISENAR.
    Decision del autor 2026-07-31, revirtiendo la del 30 tras ver los numeros
    en juego (planilla AB, check AB8, en FALLA las dos rondas).
    EL NUMERO QUE LO MATO: el MCX 5.56 del autor pesa 2,9 kg y lleva DOCE
    attachments de EFT. Con el nominal plano de 0,3 kg eso da 3,6 kg solo de
    piezas — las partes pesarian mas que el arma. No es un problema de
    calibracion sino de MODELO: en una build EFT la mayoria de los slots
    llevan ESTRUCTURA (receiver, cañon, guardamanos, tapa, miras de hierro),
    que ES el arma y no carga colgada de ella.
    LA FORMA QUE PROPUSO EL AUTOR: cobrar solo las categorias que STALKER
    Anomaly/GAMMA cobra — opticas y miras, silenciadores, lanzagranadas
    acoplables, dispositivos tacticos (laser, linterna, foregrip) y cargadores
    ampliados o de tambor. Nunca la estructura.
    LO QUE HAY QUE RESOLVER: como se AUTODETECTA esa clasificacion sobre un
    pack de terceros sin catalogar a mano cada att (que es exactamente el
    trabajo que CRG-41 existe para evitar). Candidatos a mirar en dev/other/:
    la Category del slot que lo acepta, ATT.SortOrder, y los campos de stat
    que el propio att declara. Y un peso por att declarado por el pack seria
    mejor que cualquier nominal — hay que ver si alguno lo expone.
    ENGANCHE YA ESCRITO: Instances.AttsWeight existe, recurre y esta probada
    offline; WeightOf no la llama. El filtro por categoria se enchufa ahi y en
    ningun otro lado (CRG-66: una sola recursion).
    RESTRICCION QUE DEJO EL #56, y hay que respetarla o se cuenta dos veces: las
    BALAS adentro del cargador ya pesan (CRG-67, Instances.ClipWeight). Asi que
    el peso que este bloque le de a un cargador ampliado o de tambor tiene que
    ser el del cargador VACIO. Los dos son terminos distintos de la misma suma,
    no el mismo termino.
    Y UN DATO MEDIDO QUE ESTE BLOQUE VA A NECESITAR: 134 attachments de los packs
    instalados declaran ATT.ClipSize, o sea que en EFT la capacidad del arma ES
    una propiedad del cargador montado y no de la clase (las EFT no declaran
    SWEP.ClipSize: cero lineas en los tres packs de dev/other/).
    ANTES QUE ESTE, el autor quiere el PESO DE LA MUNICION cargada: es el #56,
    abajo. Se ejecuta primero.

56. EL PESO DE LA MUNICION CARGADA [Cargo weight/ammo] — CERRADO 2026-08-01,
    entry 65 [APLICADO], planilla AC en cuatro rondas.
    CIERRA CON UNA FRONTERA COSMETICA DECLARADA en §13 y decidida por el autor:
    el HUD anuncia la municion que el arma del engine regala aunque el jugador
    nunca la reciba. El invariante esta cerrado (el pool queda en 0, medido);
    lo que miente es el aviso, y taparlo costaria no dejar que el engine equipe
    el arma — la superficie de compat que el header de capture.lua protege.
    DE PASO CERRO DOS PUERTAS DEL ETER que no eran de este bloque: equipar un
    arma del engine y tomarla del suelo, las dos regalaban reserva, la segunda
    desde el Bloque B. Pedido del autor 2026-07-31, al cerrar el #53.
    SE EJECUTO ANTES DEL #55, como el autor pidio.
    Semilla: dev/PROMPT_cargo_56_peso_municion.txt. Sede del diseno:
    Cargo_Architecture.md §16.10 + CRG-67 en ids.yaml. Detalle: CHANGELOG 65.

    LO QUE QUEDO DECIDIDO Y MEDIDO, para no releer la semilla:
    · El tipo HL2 de un arma SIN entidad viva sale de su CLASE (Ammo.TypeOfClass).
      El candidato de la semilla —Primary.Ammo— resulto FALSO para ARC9: esta
      vacio en la clase y solo Initialize lo corrige por instancia.
    · Cadencia: el poll de 4 Hz QUE YA CORRIA (voto del autor con los cuatro
      costos medidos). Recalcular el peso cuesta 1,5 us; el Touch que dispara
      cuesta 0,158 ms de disco y ~1,7 KB de red — lo caro nunca fue la
      aritmetica. Techo: 4 Touch/s disparando, 0 cuando no.
    · La opcion "pesar por CAPACIDAD" quedo descalificada con medicion: las EFT
      no declaran SWEP.ClipSize (0 lineas en los tres packs) — la capacidad vive
      en el cargador montado, o sea que necesitaba el arbol de atts del #55.

    EL SINTOMA, EN UNA LINEA: las mismas 30 balas pesan 0,36 kg en el cinturon
    y 0 kg adentro del arma. Recargar es un descuento de peso. Con varias armas
    guardadas, cada una con su cargador lleno, el jugador gana capacidad de
    carga gratis: medido sobre el loadout real del autor (planilla AC, dump de
    AC3), cinco armas escondian 1,588 kg.
    OJO — EL EJEMPLO DEL RPG QUE ESTA ENTRADA CITABA ERA FALSO, y se deja dicho
    en vez de borrado: "un cohete pesa 3,0 kg asi que cargar el RPG hace
    desaparecer tres kilos" salio de multiplicar dos campos del catalogo sin
    abrir el arma. El RPG de HL2 NO TIENE CARGADOR (Clip1 = -1, dispara de la
    reserva), asi que ahi nunca desaparecio nada. Lo refuto el check AC2.

    NO ES UN OLVIDO, ESTA DECLARADO: la nota de CRG-66 en ids.yaml lo dice
    desde el 2026-07-30 ("CASO DECLARADO Y NO CUBIERTO"). Inventory.TotalWeight
    suma grid, equipado y cinturon —los stacks del cinturon SI pesan— y no
    mira blob.clip1, que es un entero pelado sin tipo de municion.

    NO ES EL #55 Y NO SE MEZCLA: el #55 pesa el CARGADOR como pieza, este pesa
    las BALAS que tiene adentro. Mezclarlos deja los dos sin poder medirse.

    LO QUE BLOQUEA EL DISENO, y por eso la semilla es de investigacion: el def
    de un arma capturada NO sabe su tipo de municion — lleva `ammo.caliber`,
    que es etiqueta de display, y §16.2 declara como decision que el tipo real
    sale de la ENTIDAD. Un arma guardada en el grid no tiene entidad. Hay que
    resolver de donde sale el tipo HL2 de un arma sin entidad viva (candidato a
    VERIFICAR, no a asumir: weapons.Get(class).Primary.Ammo, la misma ruta que
    CRG-41 ya usa para la trivia) y aceptar que la respuesta puede ser "no para
    todas", que entonces es frontera declarada y no fracaso.

    LA TRAMPA CENTRAL ES DE DISENO Y ES DEL AUTOR: hoy el ledger solo se mueve
    cuando se mueve el CINTURON, y el sistema es barato exactamente por eso —
    el comentario del espejo lo dice literal, "firing does not touch the
    reserve, it drains the magazine" (ammopool.lua:260-262), y cada Touch es
    Save + Sync + Movement. Si las balas del cargador pesan, disparar empieza a
    mover el ledger. Tres formas con costos distintos: el peso sigue cada bala
    (honesto y caro), el peso se refresca en los momentos de hoy (barato, con
    un error maximo de un cargador que hay que poder DECIR), o el cargador pesa
    por su CAPACIDAD y no por su contenido (el mas barato, y descargar no
    aliviana). Se le presentan las tres con el costo MEDIDO, no estimado.

    ENGANCHE: CRG-66 manda UNA SOLA RECURSION — Instances.WeightOf. Ahi mismo
    tiene su enganche reservado el #55 (AttsWeight, escrita y sin llamar), asi
    que hay que verificar que los dos no colisionen ANTES de escribir.


57. LAS ARMAS QUE COMEN UN TIPO DE MUNICION QUE CARGO NO MANEJA [Cargo ammo] —
    CERRADO (entry 66, confirmado en juego 2026-08-07). Salido de una MEDICION
    del #56, no de un reporte: el censo de tipos sobre dev/other/ (243 SWEPs,
    herencia resuelta).
    QUEDA UNA SOLA COSA, Y ES DE ARTE: la caja de .308 para el item de
    Winchester. El ammo_winchester.mdl actual dice ".44 Magnum" en su arte y el
    item se etiqueta .308 (decision del autor con el desajuste a la vista); es
    SUPLENTE DECLARADO y se re-viste con Items.SetModel, sin tocar la tabla
    AMMO. No bloquea nada: los tres pools se alimentan hoy.

    RE-CENSADO SOBRE EL ARSENAL VIVO como el propio punto pedia (380 .gma
    suscritos, indice parseado y sus .lua leidos, + los binarios del engine), y
    el censo CORRIGIO DOS PREMISAS DE ESTE MISMO PUNTO:
      * "Winchester" NO ES UN TIPO DE MUNICION DEL ENGINE — no esta en
        garrysmod/bin/win64/server.dll. Es el PrintName de TFA Base para el pool
        AirboatGun (tfa_ammo_winchester, AmmoType = "AirboatGun", 50 balas). O
        sea que "AirboatGun y Winchester" nunca fueron DOS tipos: son UNO visto
        por sus dos nombres, y registrarlos como dos habria acunado un item
        fantasma.
      * Los francotiradores de ARC9 comen SniperPenetratedRound y son DIEZ
        clases, no siete: las 7 de ARC9MW mas awp/scout/ssg08 de arc9_go, que
        dev/other/ no tenia. SniperRound es OTRO pool, con tres comedores todos
        de VJ (weapon_vj_ssg08 y el m40a1 y el csniper de Half-Life Resurgence).
      * Bonus que solo aparece midiendo: ARC9MW escribe el tipo con DOS grafias
        segun el archivo (SniperPenetratedRound / sniperPenetratedRound). La
        resolucion ya era case-insensitive; si no lo fuera, la mitad de las diez
        seguiria sin cinturon y se leeria como "a veces anda".

    LA PASADA EN JUEGO (2026-08-07) CERRO POR EL CAMINO QUE SE ESCRIBIO: no se
    rompio nada, los modelos se ven, y tomar la caja de TFA entrega un ITEM AL
    GRID. Eso ultimo se PREGUNTO antes de escribirlo porque "la caja da
    municion" es cierto en los DOS mundos posibles —el item al grid (lo nuestro)
    y el pool crudo que el espejo de 4 Hz absorbe al cinturon (la deuda de
    arc9_ammo, §16.5)— y solo uno prueba que la linea nueva hizo algo. Regla:
    UN "FUNCIONA" QUE NO DISTINGUE QUE MECANISMO CORRIO NO CIERRA UN BLOQUE.
    Y dos cosas dichas, no disimuladas: NINGUNA arma del arsenal come AirboatGun
    —su unica fuente es la caja de TFA, por eso entra a WORLD_AMMO—, y los pools
    propios que cada pack ARC9 acuna por lanzable (arc9_cod2019_knife,
    arc9_cod2019_c4, los arc9_go_nade_*) quedan AFUERA: son pregunta de
    throwables (§16.9), no de cinturon.

    DIEZ CLASES resuelven a un tipo de engine que Cargo no tiene registrado como
    item: SIETE marksman/snipers de ARC9MW en "SniperPenetratedRound" (kar98k,
    m14, spr208, ax50, hdr, rytec, svd), el cuchillo arrojadizo de MW en
    "arc9_cod2019_knife", y el ssg08 de VJ en "SniperRound".

    LA CONSECUENCIA NO ES DEL #56 SINO ANTERIOR A EL, y es mas grande que el
    peso: si el tipo no esta en CARGO.Ammo.TYPES, el espejo de §16.3 no lo
    mira — o sea que EL CINTURON NO ALIMENTA A ESAS ARMAS. El #56 solo lo hizo
    visible (su cargador pesa 0) y lo dejo declarado como frontera.

    DECIDIDO POR EL AUTOR (planilla AC, nota del check AC6, 2026-08-01): se
    REGISTRAN COMO ITEMS PROPIOS, no se remapean. Textual: "hay que dar a Cargo
    los items de sniper y airboatgun/winchester ammo, hay varias armas de
    distintas bases que se alimentan con esas balas. Amerita buscar un modelo
    para esas balas". O sea el mismo trabajo que la tabla AMMO de
    shared/corpus_cargo_ammo.lua ya hace para los nueve de HL2: peso, max_stack,
    valor, calibre de display y modelo.
    Y LA NOTA SUMA DOS TIPOS QUE EL CENSO DE dev/other/ NO HABIA VISTO:
    AirboatGun y Winchester. El censo miro los SWEP de los packs instalados; el
    autor tiene armas de OTRAS bases que comen de esos pools. Antes de escribir,
    volver a censar sobre el arsenal vivo con cargo_dev_dump_weapons y no sobre
    dev/other/, que cubre unos dos tercios.
    OJO CON ESA INSTRUCCION, medido al ejecutarla: cargo_dev_dump_weapons SIN
    argumento vuelca SOLO ARC9 (el volcado de dev/weapon_dump.txt son 369 filas
    y las 369 son arc9_*), asi que por definicion no puede mostrar los pools de
    TFA ni de VJ — que es justo donde estaban AirboatGun y SniperRound. Hay que
    correrlo con "all". El censo del 2026-08-06 no lo uso: fue sobre los .gma
    suscritos, parseando el indice de cada uno y leyendo sus .lua.
    LO QUE QUEDA ABIERTO: el MODELO de cada tipo nuevo. Los nueve de HL2 usan
    el .mdl de su propio item_ammo_* y por eso una caja en el suelo y su item se
    ven igual; para un tipo que no tiene entidad de mundo en HL2 no hay ese
    default gratis.

    ENLAZA: el campo def.ammo.att reservado en el schema (§16.6) y el binding de
    ammo-atts de EFT, que es el mismo frente por el otro lado.
    Y UNA FRONTERA MEDIDA QUE VA CON ESTE BLOQUE: ARC9MW tiene CUATRO
    attachments que setean ATT.Ammo y por lo tanto CAMBIAN el tipo del arma
    (mw19_ammo_types.lua — dos a "xbowbolt", que Cargo si maneja, y dos a
    "arc9_cod2019_nade_rock", que no). §16.6 midio que ningun ammo-att de EFT lo
    hace y es cierto; lo que no se puede es generalizarlo a los otros packs.

58. EL TOOLTIP MUESTRA EL PESO PELADO DEL DEF [Cargo ui] — ABIERTO, CHICO.
    Anotado al cerrar el #56. corpus_cargo_tooltip.lua pinta def.weight, o sea
    el peso de la def sin nada de lo que la instancia lleve encima: un RPG
    cargado suma 9 kg al total y muestra 6 en su ficha.
    NO ES NUEVO — un chaleco con dos placas ya se comportaba asi desde el
    Block 1, porque WeightOf recurre y el tooltip no —, pero el #56 lo vuelve
    mas visible porque ahora cambia SOLO, sin que el jugador monte nada.
    PEDIDO EXPLICITO DEL AUTOR (planilla AC, nota del check AC3, 2026-08-01):
    "el tooltip deberia reflejar el peso que tiene el arma por efectos de la
    municion en el cargador". Deja de ser anotacion nuestra y pasa a ser pedido.
    LO QUE HAY QUE DECIDIR: si la ficha muestra el peso EFECTIVO de la instancia
    (def + sub-slots + balas, y manana + atts) o el de la def con el extra al
    lado. Es una decision de UI y del autor. El calculo ya existe y es uno solo:
    Instances.WeightOf — pero es SERVER, asi que el numero tiene que viajar en
    el snapshot o el cliente tendria que recalcularlo, que es exactamente la
    clase de segunda verdad que CRG-56/57 evitan.

59. LA MARCA DE SOBRELLENADO EN EL STATUSPANEL [Cargo ui] — CERRADA el
    2026-08-21, el mismo día que se escribió. Baja CRG-68 a código y NO acuña
    nada nuevo.
    PASADA EN JUEGO OK, 7/7 EN UNA RONDA: planilla AJ
    (dev/checks/cargo-sobrellenado-r1.html), PASA 7 · FALLA 0 · SIN CORRER 0.
    CHANGELOG 77 [APLICADO 2026-08-21].
    ⭐ LA FILA ESTRELLA CERRÓ POR SU CRITERIO Y NO POR EL QUE SE LE PARECE: no
    era «se ve el exceso» sino que 138 y 149 se vieran DISTINTO, que es el
    defecto exacto que la entrada cierra. La nota del autor trajo LOS DOS
    números —«sí, se ve el rayado en rojo y +38; también el +49 en AJTEST»—, o
    sea que la cifra discrimina y el tramado acompaña. No hizo falta
    repreguntar por la mitad que se advertía que suele faltar.
    LA DEGRADACIÓN TAMBIÉN EN VERDE, y es lo que desbloquea a Craving sin
    acoplarlo: las cuatro barras que ya existían pintan exactamente igual que
    antes. Y la fila de alcance confirma que la barra de prueba quedó
    DESMONTADA — no hay una «AJ Test» fantasma esperando en una ronda futura.
    NINGUNA NOTA ABRIÓ NADA, que es lo esperable de una entrada cuyo diseño
    había cerrado trece días antes y cuya bajada no tenía que decidir nada.
    CRG-68 deja de ser INTENCION en ids.yaml: pasa a tener harness, código,
    verificación en negativo y planilla.
    Diseño CERRADO el 2026-08-08 en Cargo_Architecture.md §11.1 (CRG-68); la
    bajada a codigo es la entry 77 y NO acuña nada nuevo. Pedido POR CRAVING,
    cuya enmienda del mismo dia
    le da stats que pasan de 100 y llegan a 150 (comer de mas es posible y
    tiene riesgo de vomito, su §2.1).
    EL DEFECTO ESTA MEDIDO, NO SUPUESTO: el poll de StatusPanel.Build clampea
    a 0..100, asi que un valor de 138 NO desborda la barra — la deja LLENA Y
    MUDA. El jugador ve lo mismo con 100 que con 149, y con 149 la proxima
    mordida lo hace vomitar casi seguro. Es peor que un desborde: un desborde
    se ve.
    QUE ENTRA: dos campos opcionales en el spec de RegisterBar (softMax,
    hardMax, + overfillColor) y el exceso pintado como TRAMADO sobre la barra
    llena — nunca alargandola, que mentiria sobre que es "lleno" —, recortado
    con render.SetScissorRect (CRG-28), y la cifra "+38" junto al label porque
    ninguna señal va solo por color ni solo por textura.
    LO QUE LO HACE BARATO: degrada en las DOS direcciones. Un registrante viejo
    no declara los campos y pinta como siempre, al pixel; un Cargo viejo ignora
    campos que no conoce. Ninguna mitad necesita saber la version de la otra, y
    por eso esta entrada NO bloquea a Craving.
    CRG-68 ES EL LIMITE: el panel dibuja MAGNITUD, jamas significado. softMax y
    hardMax son data del registrante — Cargo no sabe que es un vomito, ni que
    exista. Es CRG-1 en la unica superficie donde la tentacion es real, porque
    aca el dato llega con una consecuencia pegada.
    DOS CORRECCIONES QUE SALIERON DE LA BAJADA, y las dos de leer el arbol:
    (a) overfillColor defaultea a T.Colors.orange y NO a T.Colors.warn — la
    paleta viva no tiene esa clave; su señal fija de advertencia es `orange`, que
    queda clavada bajo cualquier teñido de DGL4. (b) La aritmetica salio del
    Paint a TRES funciones puras (BarFrac / OverFrac / OverLabel): un PaintOver
    es una closure sin nombre y sin superficie que dibujar offline, asi que lo
    unico que separa «llena y muda» de «llena y hablando» habria sido lo unico
    que ningun check podia alcanzar.
    PENDIENTE: la pasada en juego (planilla AJ, 7 filas; su fila 02 son DOS
    numeros —138 contra 149— y el criterio es que se vean DISTINTO).
60. CONTAR Y CONSUMIR CONTRA CONTENEDORES CERCANOS [Cargo containers] — PEDIDO
    DE COAGULANT, SIN RATIFICAR POR CARGO. Abierto 2026-08-09.
    QUIEN PIDE Y POR QUE: Coagulant vota su "area hospital" (COA-50, su §17). La
    medicina de hospital no asume una mochila, asume una farmacia: el can() de
    una accion tiene que poder mirar los contenedores dentro de un radio, no
    solo el inventario del medico.
    QUE PIDE, y es el espejo EXACTO de las tres que Coagulant ya usa para que no
    haya que aprender un modelo nuevo:
        CARGO.Containers.AreaHas(pos, radius, id)      -> bool
        CARGO.Containers.AreaCount(pos, radius, id)    -> n
        CARGO.Containers.AreaTake(pos, radius, id, n)  -> ok
    LAS TRES, NO DOS, por una leccion que este repo ya pago: CountItem cuenta
    solo stacks y los unique le son invisibles — es por que el torniquete daba
    siempre 0 (fallo G4 de Coagulant, 2026-07-13) hasta que Cargo agrego
    HasItem. Un AreaCount sin su AreaHas reproduce el defecto en el area, con el
    mismo sintoma: la accion existe, el item esta, y el boton dice que no hay.
    POR QUE NO LO RESUELVE COAGULANT SOLO: el contrato publico de Cargo expone
    unicamente Containers.Attach; el contenido vive en _byId/Snapshot, que es
    off-contract. Coagulant YA arrastra una deuda de esa clase — cuenta los
    items de sus botones con CARGO.ClientState.items, con la nota escrita de que
    se rompe EN SILENCIO si Cargo cambia su snapshot. Firmar la segunda por
    comodidad convierte un accidente en metodo.
    ESTADO: PEDIDO. Se escribe el mismo dia en los dos lados justamente porque
    D-5 dejo la leccion de que pasa cuando una firma vive un mes en un solo lado
    (ApplyExternalCondition, congelada por el consumidor y sin ratificar por el
    dueno). Hasta que Cargo la ratifique, el area hospital de Coagulant NO baja
    a codigo. Si Cargo la rechaza o la devuelve con otra forma, manda Cargo: la
    superficie es suya.
    LO QUE ESTA ENTRADA NO PIDE, para que no se lea de mas: nada de permisos.
    Que OpenFor no compruebe dueno (cualquiera a 160 u abre cualquier
    contenedor) es un hecho de Cargo que el area hospital ENSANCHA — de 160 u a
    lo que mida la sala — pero no crea. Vive en el #12, no aca.
61. MULTIPLICADOR DE PRECIO POR CATEGORIA [Cargo trade/items] — RATIFICADA EN
    RUMBO el 2026-08-18, SIN ESCRIBIR. Abierta 2026-08-17.
    RATIFICACION: el autor la voto en la ronda de diseño del trader de
    corpus-stalker, eligiendola sobre la 62 y sobre "ninguna de las dos". Lo
    ratificado es el RUMBO y la forma propuesta (que RegisterCategory cree
    `cargo_value_mult_<id>`, ARCHIVE + REPLICATED); los TRES SUB-VOTOS de mas
    abajo siguen abiertos y se resuelven en la ronda que la escriba.
    POR QUE ESTA ES Y LA 62 NO: es la que la comida NECESITA. Sin ella los 15
    comestibles quedan en 2-12 USD contra municion de 8-900 y armas de miles, o
    sea economicamente triviales. Lo que un trader VENDE ya lo decide la entidad
    al llamar AttachTrader, asi que la 62 no la usa ningun tramo abierto.
    DE DONDE SALE: de ponerle precio a los 15 comestibles de Craving para que
    el trader de comida de corpus-stalker pueda venderlos. Con precios de la
    vida real en USD (pan 1,81/libra, gaseosa 2,20 los 2 litros, leche 4,32 el
    galon — FRED, jun-2026) las 15 defs caen TODAS en la banda 2-6, contra
    municion de 8 a 900 por ronda y armas de miles. La comida queda
    economicamente trivial: nadie decide nada por 2 dolares.
    EL PEDIDO, en palabras del autor: "agreguemos un factor de multiplicacion
    con un cvar a cargo, cosa que puedan encarecer distintas cosas, algo asi
    como cargo_value_mult_food 10 (...) podriamos aplicar eso para toda
    categoria, asi se multiplica la municion, armas, armaduras, misc, etc. Y es
    mas sencillo digo yo para manejar la economia general del server o mapa".
    POR QUE ES DE CARGO Y NO DEL QUE PIDE: el precio es de Cargo (CRG-18/19/20)
    y la matematica vive en UN solo lugar, shared/corpus_cargo_trade.lua. Un
    addon de contenido que quisiera encarecer su comida tendria que reescribir
    los value de defs ajenas — que es exactamente lo que STK-1 le prohibe.
    DONDE ENTRA, MEDIDO: Trade.UnitPrice es el unico punto donde se computa
    precio, y lo usan las DOS puntas (el server al confirmar, el cliente al
    pintar la celda y el total del basket). El multiplicador va ahi, sobre el
    `value` base y ANTES del spread y del redondeo:
        precio = round(value x mult_categoria x condicion x spread)
    Sobre el value y no sobre el precio final para que el spread siga operando
    sobre la magnitud escalada.
    LA CONDICION QUE NO ES OPCIONAL — REPLICADA. El cliente pinta el precio con
    la MISMA funcion pura que el server cobra (es el diseño de §4, y es lo que
    permite que el basket sea intent puro). Una convar de servidor sin
    FCVAR_REPLICATED deja al cliente multiplicando por su propio valor: la celda
    diria un numero y el Confirm cobraria otro, y el jugador leeria eso como que
    el trader lo estafa. Es la misma clase de defecto que CRG-18 evita, entrando
    por la puerta de atras.
    LA FORMA QUE SE PROPONE, y por que no la obvia: las categorias son un SET
    ABIERTO (Items.RegisterCategory lo puede llamar cualquier addon; hoy son 10
    propias + food de Craving). Una lista fija de convars no cubre la que
    registre un addon mañana, y una sola convar con un string "food:10,ammo:2"
    obliga a parsear, no autocompleta en consola y no tiene default por entrada.
    La forma que sale sola del registro: que RegisterCategory CREE la convar de
    su categoria, `cargo_value_mult_<id>`, default 1.0, ARCHIVE + REPLICATED.
    Toda categoria — presente y futura — nace con su perilla y nadie mantiene
    una lista. El nombre que propuso el autor es ademas el correcto: multiplica
    el `value`, no el `price`, que son dos cosas distintas en este repo.
    SUGERENCIA ENCIMA, no pedida: una global `cargo_value_mult` (default 1.0)
    que componga multiplicativamente con la de categoria. Da "toda la economia
    x2" en una perilla sin tocar diez, que es la mitad de lo que el autor
    describe ("manejar la economia general del server o mapa").
    LO QUE HAY QUE VOTAR ANTES DE ESCRIBIR:
      a) Que significa mult 0. Hoy UnitPrice hace max(1, ...), asi que un 0
         dejaria TODO a 1 en vez de sacarlo de venta. Las dos lecturas son
         defendibles y son opuestas: "gratis" contra "no se vende" — y esta
         segunda ya tiene dueño en el repo (ausencia de value). Un 0 que caiga
         en la lectura equivocada es un exploit, no un ajuste.
      b) Si el multiplicador afecta tambien lo que el trader PAGA (buy_mult) o
         solo lo que cobra. Componiendolo sobre el value afecta a los dos por
         construccion, que probablemente es lo que se quiere — pero conviene
         decirlo, porque significa que subir food x10 tambien hace que vender
         comida al trader rinda x10.
      c) Donde queda el default de 5 USD para comestibles sin precio propio: NO
         es de Cargo (Cargo no inventa el value de una def ajena, CRG-1). Vive
         en quien arma el stock del trader, o sea en corpus-stalker.
    LO QUE ESTA ENTRADA NO PIDE: ningun cambio en como se declara `value` en una
    def, ni que Cargo conozca ninguna categoria en particular. La perilla es
    ciega al significado — es CRG-1 otra vez: Cargo escala una magnitud y no
    sabe que "food" es comida.
62. QUE UN TRADER ELIJA QUE COMPRA [Cargo trade] — PEDIDO DEL AUTOR, SIN
    DISEÑAR. Abierto 2026-08-17.
    DE DONDE SALE: de la misma sesion que el 61. El autor, mirando la regla
    "sin value no se vende", se pregunta en voz alta si no seria mejor al reves
    — "dejar que se regale algo de 0 USD y al trader aplicar filtros sobre lo
    que este quiera y no quiera comprar". No es un voto: es una pregunta, y se
    anota como tal.
    LA MITAD QUE NO ES DE CARGO, PARA NO CONFUNDIRLAS: que un trader VENDA solo
    ciertas categorias no necesita nada de acá — lo decide quien arma el stock
    al llamar AttachTrader, o sea la entidad. Los dos traders que corpus-stalker
    va a tener (comida uno, municion+medicina+algunas armas el otro) se
    resuelven enteros de ese lado.
    LA MITAD QUE SI ES DE CARGO: lo que un trader ACEPTA del jugador. Hoy
    compra cualquier cosa que tenga value, y el filtro no existe. Trade.Confirm
    es donde se validaria, y tendria que ser el MISMO filtro que el cliente usa
    para pintar/deshabilitar la celda — si no, el jugador arma el carrito y el
    Confirm se lo rechaza entero sin decir cual linea sobra (peor UX que no
    tener filtro).
    LO QUE HAY QUE VOTAR, y son dos cosas independientes que conviene no
    mezclar:
      a) Semantica de `value = 0` DECLARADO, que hoy no existe como caso: la
         ausencia ya significa "no esta a la venta" (contrato 13). Un 0
         explicito podria significar "regalo" — pero ojo con la simetria: si el
         trader lo REGALA, tambien lo COMPRA a 0, y un item de valor 0 que se
         puede vender infinitas veces no rompe nada… salvo que ocupe grid del
         trader. La lectura "0 = gratis" solo cierra si viene acompañada del
         filtro de compra de (b).
      b) La forma del filtro: lista blanca de categorias, lista negra, o un
         predicado. Las tres se pueden expresar en opts de AttachTrader; la
         diferencia esta en que pasa con una categoria que NO estaba prevista
         (una que registre un addon mañana) — la blanca la excluye sola, la
         negra la incluye sola, y esa es toda la decision.
    LO QUE ESTA ENTRADA NO PIDE: nada sobre precios. El multiplicador es el 61 y
    son independientes — se pueden hacer en cualquier orden o solo uno.

63. ENUMERAR EL CATALOGO DESDE AFUERA DEL MODULO [Cargo items] — RATIFICADA Y
    ESCRITA el 2026-08-18. Abierta y cerrada el mismo dia.
    DE DONDE SALE: de sentarse a escribir el trader de comida de corpus-stalker
    (su roadmap [1]) y descubrir que la regla ya votada por el autor —"vende
    TODO lo que declare category = food", que es una REGLA y no una lista, para
    que una comida registrada mañana por cualquier addon entre sola— NO SE PUEDE
    ESCRIBIR. `Items.Get(id)` responde por UNA def y no existia forma publica de
    recorrerlas. Medido con `rg --no-ignore` sobre el workspace entero: los
    unicos tres recorridos son dev.lua, lan.lua e iconeditor.lua, y los tres
    leen `CARGO.Items._defs` directo porque viven ADENTRO del modulo; ningun
    consumidor de afuera la tocaba.
    POR QUE ES DE CARGO Y NO DEL QUE PIDE: STK-1 le prohibe a corpus-stalker
    agregar superficie a este modulo, y alcanzar `_defs` desde afuera no evita
    el acoplamiento — lo muda a una tabla privada, que es peor porque se rompe
    en silencio. Es el mismo argumento con el que este repo ya movio
    Trade.StockOf / HasViewer / ClearViewers a accesores publicos cuando la
    entidad de Sidorovich empezo a leer `trader.cont` y `trader.viewers`.
    VOTO DEL AUTOR, textual, cuando se le presento el hallazgo: "si no hay API
    mejor construirla correctamente". Eso ratifica la entrada; el trader queda
    esperandola y su prompt, pendiente.
    LO QUE SE ESCRIBIO, en shared/corpus_cargo_items.lua:
        CARGO.Items.GetAll()            -> lista de defs, ordenada por id
        CARGO.Items.ByCategory(id)      -> las de esa categoria, ordenada por id
    Lista FRESCA (del caller, mutable), defs POR REFERENCIA — mismo invariante
    que Items.Get, el modulo dueño las sigue escribiendo. No abre ninguna via
    para mutar el registro: no hay desregistro publico y esto no lo inventa.
    EL ORDEN ES LA MITAD DEL CONTRATO, y es lo unico que no era obvio. `_defs`
    es un hash y el orden de `pairs` no se repite entre sesiones: un trader que
    siembre stock recorriendo el registro arma un catalogo DISTINTO en cada
    arranque sin que nadie lo haya sorteado, y un defecto que dependa del orden
    no se reproduce dos veces — se lee como mala suerte, que es la peor forma
    que puede tomar. Acuñada como CRG-69, sede Cargo_Architecture.md §3.
    LO QUE NO HACE, para que no se lea de mas: no oculta el catalogo BULK
    (attachments de ARC9, armas capturadas wpn_*, las 61 NVG). Esconderlas es
    decision de DISPLAY y vive en el listado de dev.lua, que ya tiene su
    IsBulk; un accesor que descartara entradas mentiria sobre que hay
    registrado, y el que siembra stock decide por si mismo.
    HALLAZGO DE INSTRUMENTO QUE SALIO DE ESCRIBIR ESTO, y es mas grande que la
    entrada — ver CHANGELOG 67: harness_cargo.py invocaba el selftest con
    `pcall` y miraba SOLO si habia corrido, tirando el valor de retorno, que es
    justamente `fail == 0`. Con el selftest en ROJO el harness imprimia
    ALL GREEN y salia 0: sus 86 checks de server y 93 de client no hacian
    fallar nada. Los harness de Craving y Coagulant ya exigian el `== true`;
    el de Cargo era el unico. Arreglado en el mismo parche.
    NO CONFUNDIR CON EL 61 NI CON EL 62. El 61 (multiplicador de precio por
    categoria) y el 62 (que un trader elija que compra) siguen PEDIDOS sin
    ratificar y son independientes de esto: el 63 es leer el catalogo, no
    preciarlo ni filtrarlo.

64. EL ITEM DE UNA CLASE DE ARMA [Cargo capture/items] — RATIFICADA Y ESCRITA
    el 2026-08-18. Hermana del 63 y del mismo dia siguiente.
    DE DONDE SALE: de retomar el trader de corpus-stalker con el 63 ya escrito
    y chocar con el MISMO agujero en otra superficie. La regla por categoria ya
    se podia escribir, pero las ARMAS no estaban EN el catalogo: un arma
    capturada no tiene codigo propio —su def es AUTOGEN— y hasta hoy la acuñaba
    SOLO la captura, cuando el engine entregaba el arma. `EnsureDef` es una
    funcion LOCAL de server/corpus_cargo_capture.lua: no habia puerta publica y
    desde afuera ni siquiera se puede alcanzar.
    EL SINTOMA, QUE ES EL QUE IMPORTA: `Trade.AttachTrader` resuelve cada linea
    de `opts.stock` con `Items.Get`, no encontraba la def, logueaba "stock
    desconocido" y salteaba EN SILENCIO. O sea que el trader en un server
    FRESCO vendia CERO armas con el pack ARC9 EFT montado entero — y eso se lee
    exactamente igual que "el pack no esta montado". Un falso negativo que
    acredita al pack.
    VOTO DEL AUTOR al presentarle el hallazgo, textual: "Si es bloqueante
    paremos aca, guarda el prompt original, y solucionemos la bloqueante de
    cargo". El trader vuelve a quedar en pausa y esto se escribe primero.
    Y PREGUNTO SI PASA LO MISMO CON EL RESTO: no. Medido con `rg --no-ignore`
    sobre TODOS los `Items.Register` del workspace — comida (Craving), medicina
    (Coagulant), municion y suministros (Cargo), las 61 NVG y los attachments
    de ARC9 se registran EN EL ARRANQUE desde archivos shared, y existen
    mientras su addon este montado. Las armas capturadas `wpn_*` son la UNICA
    familia acuñada en runtime: `autogen = true` aparece una sola vez en todo
    el arbol de Cargo. El agujero era de una familia, no del catalogo.
    LO QUE SE ESCRIBIO, en server/corpus_cargo_capture.lua:
        CARGO.Capture.ItemIdFor(class)  -> id | nil, motivo
    Acuña la def autogen si no existe y devuelve su id; si la clase ya tiene
    cara canonica (frag/SLAM, roadmap #32) devuelve ESA. Cuatro motivos de
    rechazo distinguibles: "invalid class", "ignored class", "unknown weapon
    class" (las armas del ENGINE no son SWEPs) y "NPC-only weapon".
    LO QUE ACUÑA LO PERSISTE, y es la mitad que no era obvia: `autogen_defs`
    existe (CHANGELOG #6) para que un arma CAPTURADA sobreviva al cambio de
    mapa, y un arma COMPRADA necesita exactamente lo mismo. Efecto lateral
    declarado: el catalogo pasa a ser "lo que los jugadores tuvieron en la mano
    Y lo que los traders sortearon".
    DOS DATOS QUE ANTES SALIAN DE LA ENTIDAD VIVA y ahora salen de la clase: el
    PrintName (trepando `.Base` con GetStored, que NO hereda) y el calibre (via
    Ammo.TypeOfClass). Sin la trepada, una clase que no declara PrintName
    propio produce un item LLAMADO COMO SU CLASE y nada falla — la misma falla
    sin sintoma que midio el censo de dev/other/ (9 de 99 SWEPs spawneables
    heredan el campo).
    Acuñada como CRG-70, sede Cargo_Architecture.md §3.
    HALLAZGO DE INSTRUMENTO DEL MISMO PARCHE, y salio de verificar en negativo:
    la primera version del tramo de rechazos era CIEGA. Borrando el gate
    `Ignore` la pasada seguia VERDE, porque el check solo exigia "rechazado con
    algun motivo" y la clase caia un gate mas abajo —el de existencia— y se
    rechazaba igual. Un check que no mira CUAL gate contesto no juzga ninguno:
    lo aprueba el de al lado. Reescrito comparando el motivo exacto, y la clase
    de `Ignore` sembrada en el padron para que el gate de abajo no pueda
    cubrirla. Los 8 gates verificados en negativo, uno por uno.
    Harness: 838 -> 852 verdes; selftest server 86 -> 91.
    PASADA EN JUEGO OK el 2026-08-18: cargo_selftest 91 OK / cargo_selftest_cl
    93 OK, 0 fallas en los dos, y los dos totales COINCIDEN con lo que el
    harness offline habia predicho — que es lo unico que acredita al harness.
    Cerro en la misma corrida el #63. CHANGELOG 67 y 68 [APLICADO].

65. CERRAR LA PANTALLA DE TRADE DESDE EL SERVER [Cargo trade] — RATIFICADA el
    2026-08-18, SIN ESCRIBIR.
    DE DONDE SALE: del diseño del re-sorteo del trader de corpus-stalker
    (§-2.H de su handoff). El autor voto que el trader, en los ultimos N
    segundos antes de re-sortear, EXPULSE al que este comerciando, avise en
    pantalla y lo diga con una linea de voz — para que el restock sea
    inbloqueable y ademas VISIBLE, en vez de un evento de fondo.
    MEDIDO ANTES DE PEDIRLA, y es lo que la vuelve una entrada y no un capricho:
    el net `trade_close` es CLIENTE→SERVER UNICAMENTE (client/…:184 lo manda,
    server/…:560 lo recibe; el camino inverso no existe). O sea que hoy NADIE
    puede cerrarle la pantalla a un jugador desde el server.
    LO QUE YA ALCANZA HOY, PARA QUE NO SE LEA COMO BLOQUEANTE: `ClearViewers`
    vacia el set de viewers y a partir de ahi `ViewedTrader` devuelve nil, asi
    que el server contesta "The trader is out of reach." a cualquier intento —
    expulsion FUNCIONAL, sin tocar este repo. Lo unico que falta es que el panel
    del jugador SE CIERRE en vez de quedar dibujado hasta que lo cierre a mano.
    corpus-stalker escribe su tramo con `ClearViewers` y esta entrada lo pule.
    LA SEGUNDA COSA QUE SALDA, y es DEUDA YA DECLARADA de antes: el header de
    `corpus_stalker_sidorovich.lua` dice que si el trader MUERE con pantallas
    abiertas, quedan huerfanas y el jugador las cierra a mano — "Cargo no tiene
    trade_close server→cliente". Es la misma superficie: UNA entrada, DOS
    problemas, y el segundo lleva meses anotado como degradacion honesta.
    FORMA PROPUESTA (no votada en detalle): `Trade.CloseFor(ply, trader)` que
    saque al jugador del set de viewers Y le mande el net de cierre; y que
    `ClearViewers` pase a hacerlo para todos, en vez de solo vaciar la tabla —
    hoy su comentario declara explicitamente que deja "a stale panel".
    ORDEN: VA DESPUES DEL TRAMO DEL TRADER (voto del autor, 2026-08-18). No es
    solo que se pueda —no bloquea nada—, es que CONVIENE: el sub-voto (a) de
    aca abajo pregunta si `ClearViewers` cambia de comportamiento o si `CloseFor`
    es aparte, y quien contesta bien eso es la pasada en juego del trader, que
    va a mostrar si el panel muerto molesta y de que forma. Hacerla primero le
    da el dato a la decision en vez de adivinarlo.
    LO QUE EL TRADER TIENE QUE HACER PARA QUE ESTO NO LE CUESTE NADA DESPUES, y
    ya esta escrito en su handoff: llamar a `ClearViewers` por el helper
    perezoso `TradeAPI("ClearViewers")` que su `OnKilled` ya usa. Si esta
    entrada termina cambiando `ClearViewers`, el trader no toca UNA linea; si
    termina en una funcion aparte, es una linea y el lazy check ya esta puesto.
    LO QUE HAY QUE VOTAR ANTES DE ESCRIBIRLA:
      a) Si `ClearViewers` cambia de comportamiento (cerrar de verdad) o si se
         queda como esta y `CloseFor` es una funcion aparte. Cambiarla toca al
         unico consumidor que tiene hoy, que es el OnKilled del trader de
         corpus-stalker — y ahi cerrar de verdad es EXACTAMENTE lo que se
         queria, asi que probablemente sea una mejora y no un riesgo.
      b) Si el cierre lleva un MOTIVO que el cliente pueda pintar, o si el
         aviso lo manda el que expulsa por su cuenta con `Inventory.Notice`
         (que es lo que corpus-stalker va a hacer igual).
    LO QUE ESTA ENTRADA NO PIDE: ningun re-sync de viewers. Ese hacia falta
    mientras se pensaba re-sortear CON gente mirando; el diseño que se voto
    expulsa ANTES de re-sortear, asi que nadie mira cuando el stock cambia y el
    proximo que abra recibe un Snapshot fresco por el camino de siempre. La
    superficie `Trade.SyncFor` que se habia propuesto SE CAYO ENTERA.

66. LOS USOS DE UN ITEM (def.uses) [Cargo items/ui] — CERRADA el 2026-08-19,
    PASADA EN JUEGO OK ese mismo dia: planilla dev/checks/cargo-usos-r1.html,
    12 de 12, cargo_selftest 100 OK / cargo_selftest_cl 107 OK, 0 fallas los dos
    y los dos totales CLAVADOS con los que el harness offline habia predicho.
    Abrio la #71 (la unidad del chip). CHANGELOG 69 [APLICADO].
    DE DONDE SALE: pedido del autor el 2026-08-18, cerrando el diseno de la
    cordura de Phantasmagoria — «tener un solo item pero con x usos mas que
    varios items en un stack (...) el mismo tooltip deberia decir cuantos usos
    le queda, eso parece que le queda pendiente a Cargo».
    LO PRIMERO QUE SE MIDIO, Y ES LA MITAD DEL VALOR DE LA ENTRADA: NO estaba
    pendiente. La barrita que el pedido describe ya estaba dibujada hacia rondas
    (grid.lua, pegada al borde inferior de la celda), el tooltip ya tenia su fila
    de condicion, `has_condition` ya existia en el contrato, y el precio de un
    frasco a medio usar YA se partia al medio sin una linea escrita porque sale
    de value x condicion x spread. Lo unico que faltaba era la UNIDAD: la celda
    decia «67 %» donde un frasco de pastillas tiene que decir «2/3».
    Y el prompt que la pidio decia que la condicion se dibuja en DOS lugares:
    son TRES para el texto (celda del grid, titulo del tooltip, fila de sub-slot
    montado) y CINCO para la barra (mas la celda de equipamiento y el sector del
    wheel). El censo se hizo antes de tocar nada.
    LO QUE SE ESCRIBIO: `def.uses = 3`, entero >= 1 y SOLO en uniques, que hace
    que la condicion 0-100 se LEA como n/uses en las tres superficies de texto.
    No hay un segundo numero guardado: misma condicion, mismo precio, misma
    persistencia. La conversion son dos funciones puras en shared/items.lua
    (`Items.UsesLeft` y su inversa `Items.ConditionForUses`), asi el harness las
    mide en los dos realms y el modulo dueno gasta un uso sin restar a mano.
    LOS CINCO VOTOS DEL AUTOR (2026-08-19), todos con su motivo escrito en
    Cargo_Architecture.md §3 (sede de CRG-71):
      a) ALCANCE: opcion (b) — la unidad Y habilitar la tecla rapida en uniques.
      b) REDONDEO: TECHO. Compra «0 usos si y solo si condicion 0»; con piso, un
         frasco en 33,3 lee 0 usos mientras todavia sirve y el jugador lo tira.
      c) TOOLTIP: los usos Y el porcentaje («2/3 uses · 67 %»), porque el % es de
         donde sale el precio y esconderlo hace que una reventa a la mitad se lea
         como un error de precio. La celda, que no tiene lugar, dice solo «2/3».
      d) `uses` IMPLICA `has_condition`. Sin el, Instances.Create no siembra
         condition y el item no dibuja NADA, sin un solo error.
      e) A CERO EL ITEM QUEDA. Cargo no lo borra: que significa un frasco vacio
         es del modulo dueno (CRG-1).
    LO QUE APARECIO AL MEDIR EL ALCANCE, y es lo que volvio la (b) barata en vez
    de cara: el quick bind YA direccionaba por ID DE DEF y no por uid, que es la
    clave correcta (un uid muere con la instancia y dejaria la tecla atada a nada
    el primer frasco que se acaba). Faltaba resolver ese id a una INSTANCIA al
    apretar. La regla votada es «el mas gastado que todavia sirve» — la regla de
    STALKER —, y no es cosmetica: como Cargo no borra el item a cero, sin ella un
    frasco vacio se sienta adelante y se come cada apretada para siempre.
    DOS DEFECTOS VIVOS Y SILENCIOSOS que esa misma resolucion cerro:
      · un UNIQUE con onUse era inalcanzable por la tecla — y se podia atar igual
        ARRASTRANDOLO, porque el gate de clase estaba solo en el menu contextual
        y el receiver del drag no tenia ninguno. La tecla contestaba «You are out
        of that consumable» para siempre sobre una mochila con dos frascos. El
        Tourniquet de Coagulant (unique + onUse + «Not consumed») lleva meses en
        ese estado.
      · un STACK CON CONDICION tambien, y ese ni siquiera avisaba: CountItem lo
        contaba (pasaba el guard) y despues FindEntry fallaba por el
        condition ~= nil, asi que UseEntry devolvia false sin un solo Notice.
    LA PREGUNTA ABIERTA QUE EL PROMPT DEJO, contestada: cuando el modulo dueno
    muta blob.condition desde su onUse, el cliente se entera SOLO — UseEntry
    llama Touch DESPUES del onUse y el Sync lee el blob vivo de Instances._live.
    Fuera del onUse (un timer, dano) el dueno tiene que llamar Inventory.Touch el.
    Escrito en el header de cargo_dev_pills, que es la implementacion de
    referencia del lado del dueno.
    MEDICION: harness 852 -> 910 verdes (37 en server, 15 en cliente, 6 de un
    GATE DE FUENTES nuevo); selftest server 91 -> 100, cliente 93 -> 107.
    LOS 14 SABOTAJES VERIFICADOS EN NEGATIVO uno por uno: los 14 ponen la pasada
    en rojo. Dos de ellos NO la ponian al principio, y ese es el hallazgo de
    metodo de la tanda: sabotear el helper del theme daba rojo, pero revertir la
    CELDA DEL GRID a imprimir el % a mano dejaba todo VERDE. Se estaba midiendo
    el helper y no que alguien lo llamara. Los closures de PaintOver no tienen
    nombre por el que agarrarlos desde Lua y no hay superficie que dibujar
    offline, asi que lo unico que ve la diferencia es el TEXTO FUENTE — de ahi el
    gate, que lee los archivos y dice que mide. Un check nacio ademas en ROJO
    (estaba escrito con la intuicion del PISO, la opcion descartada) y se quedo
    en la planilla con esa nota: es la prueba de que el instrumento discrimina.
    LO QUE ESTA ENTRADA NO HIZO, a proposito: no toco el calculo de precio, no
    agrego ni un mensaje de red (el estado ya viaja; esto es render), no
    implemento el consumo (descontar es del modulo dueno, contrato #2) y no toco
    Phantasmagoria, que es el consumidor que lo pidio y va en otra sesion.
    LO QUE ESPERA DEL OTRO LADO: las pastillas de cordura de Phantasmagoria
    (§19.8 de su diseno) y, del mismo doc §10.3, crucifijo/sal/smudge, el film de
    la camara y las baterias. El reparto que ese doc escribio —«lo que se usa en
    panico va a una tecla, lo que se gasta despacio va con barrita»— YA NO HACE
    FALTA: con la (b) votada, un unique con usos tiene las dos cosas.


67. EL ORDEN DEL GRID Y EL STACK QUE UN CLIC NOMBRA [Cargo ui/inventory/trade]
    — CERRADO Y ESCRITO el 2026-08-19 (votos del autor en la misma pasada),
    PASADA EN JUEGO PENDIENTE. CHANGELOG 70.

    LO QUE PIDIO EL AUTOR: (a) que los items dejen de quedar desordenados y que
    el inventario GUARDE la posicion de cada uno, aunque el grid sea infinito
    porque el limitante es el peso; (b) que apretar sobre un stack de municion
    opere sobre ESE stack (120 balas de pistola) y no sobre las 800 que hay en
    la mochila, dejando M1 como esta (un cargador, 30) y SHIFT+M1 como el stack.

    LOS DOS PEDIDOS TENIAN LA MISMA RAIZ y no era la que parecia: una entrada
    de stack no tiene identidad (los unicos llevan uid, los stacks se nombran
    por id+condition). PERO para el comercio la identidad resulto INNECESARIA:
    dos stacks del mismo id y condition son FUNGIBLES, asi que lo que un clic
    manda no es CUAL sino CUANTO. La identidad hacia falta solo para la
    posicion, y ahi se llama `ord` y NO viaja en ningun ref.

    LA PREMISA QUE ERA FALSA, y medirla fue lo que ahorro el bloque: «tal vez a
    la municion le falta informacion para separar stacks» — no le falta.
    AddStack parte por max_stack desde el Block 1 y las 800 balas YA eran siete
    entradas de 120/.../80. Lo que faltaba no era partir: era no volver a
    sumarlas al hacer clic.

    VOTOS DEL AUTOR (2026-08-19):
      - Posicion: NIVEL 1 — orden persistido, SIN arrastrar. No hay slot de
        celda ni colision de footprints; la enmienda del 2026-07-11 («el
        footprint es solo render, sin gestion espacial») SIGUE EN PIE.
      - SHIFT+M1 = el stack clicado; CTRL+SHIFT+M1 = todo (el viejo).
      - Contenedores y stock del trader TAMBIEN parten por max_stack: una celda
        es un stack en todas las listas.

    LO ESCRITO: `ord` por entrada estampado por los DOS funnels del record
    (SaveRecord y BuildSnapshot), `Items.MaxStack` + `Items.AutoSortLess` en
    shared (una sola casa para el techo y para el criterio), boton Sort junto a
    las tabs, las tres cantidades de ClickAmount, y el derrame sobre las
    entradas que respaldan un ref en la transferencia de contenedores.
    Sede de diseno: Cargo_Architecture.md §7.2, cita CRG-72.

    DOS DEFECTOS VIVOS QUE NADIE PIDIO, y salieron de medir el alcance:
      - El comparador del grid terminaba en `(a.uid or "") < (b.uid or "")`,
        que para dos stacks compara "" contra "": empate total, y table.sort de
        Lua NO es estable. El x120 y el x80 de la misma municion se cambiaban
        de lugar entre syncs sin que nada cambiara.
      - Take all / Move all avisaba «no pude mover todo» sobre operaciones que
        habian funcionado ENTERAS: la lista de refs traia uno por entrada, el
        primero se llevaba las siete y los otros seis no resolvian nada — que
        ese loop lee como «bloqueado». Vivo desde antes de este bloque del lado
        `put`, porque el grid del jugador siempre partio.


68. EL REF DE UN STACK NOMBRA LA PRIMERA ENTRADA, NO LA CELDA QUE APRETASTE
    [Cargo inventory/ui, bug] — CERRADO Y ESCRITO el 2026-08-19 (los dos votos
    del autor tomados antes de escribir una linea). PASADA EN JUEGO OK el
    2026-08-19: los dos sintomas del reporte cerrados —el belt se lleva la celda
    que arrastras (84 balas) y siete stacks de pistola dan SIETE items al botar,
    que es justo lo que el defecto impedia—, mas los dos selftest en juego
    clavados con el offline (100 server / 107 client, 0 fallas). Sin correr
    quedaron las seis filas que el harness ya cubre offline (Sort+arrastre,
    equip, use, sub-slot y los dos controles negativos), y se dice asi porque un
    check que no se corrio no es un verde. CHANGELOG 71 [APLICADO].
    Sede de diseno: Cargo_Architecture.md §7.3, cita CRG-73.

    LO QUE REPORTO EL AUTOR (en juego, 2026-08-19): «al meter al belt, ese que
    tiene 107 se mete otro de 120» y «bote toda mi municion de pistola del grid
    y salieron 6 items en vez de los 4 que tengo en el grid».

    NO SE ESTAN CREANDO BALAS. La conservacion se cumple; lo que no se cumple
    es CUAL entrada se mueve. `FindEntry` (corpus_cargo_inventory.lua) resuelve
    un ref de stack —{ id, condition }— contra la PRIMERA entrada que empareja.
    El cliente manda ese ref desde la celda que apretaste, pero la celda no
    viaja: el server agarra la primera de la fila.
      · BeltSet: arrastras la celda de x107 y el server mueve la primera
        entrada de 9x19, que tiene 120. El slot muestra 120. Ese es el sintoma
        exacto que reportaste.
      · DropEntry: clampea `count` al `entry.count` de la PRIMERA. Botar la
        celda de 107 saca 107 de una de 120 y deja un resto de 13, que se
        convierte en un item mas en el piso. Simulado sobre [120,120,120,107]:
        5 items de piso y 467 balas — mas items que celdas, mismo total.
    CINCO caminos comparten `FindEntry` y todos dicen «la primera»: UseEntry,
    DropEntry, BeltSet, el equip y el attach a sub-slot. Los DOS que si quedaron
    bien son los que el #67 toco a proposito, porque ahi la semantica correcta
    es de CANTIDAD y no de celda: el basket del trade y la transferencia de
    contenedores.

    LA PREGUNTA DEL AUTOR — «¿cada stack de 120 podria ser un blob propio que
    sume balas hasta 120?». Se puede (es `class = "unique"` + Instances.Create
    por stack) pero es la herramienta cara y equivocada: un blob existe para
    guardar HISTORIA (condicion, attachments, clip1) y dos stacks de 9x19 no
    tienen ninguna historia que los distinga. Costaria un blob persistido por
    stack de municion en el record, rotacion de uid en cada merge y cada split,
    y romperia el espejo cinturon-pool, que esta construido sobre «N balas del
    tipo T» y no sobre «este monton». Arreglaria el sintoma, pero pagando con un
    objeto de persistencia lo que resuelve un CAMPO.

    LO QUE HACE FALTA DE VERDAD: que el ref pueda nombrar la celda. Dos formas.
      (a) Reusar `ord` (#67): ya existe, ya se persiste, ya viaja en el
          snapshot. Barato. Contra: un `Sort` re-escribe los `ord`, asi que un
          ref en vuelo queda viejo — cae al comportamiento de hoy, no peor,
          pero debilita la mitad «una posicion NO es una identidad» de CRG-72.
      (b) Un `sid` estable por entrada, separado de `ord`: se acuña una vez y
          no se reasigna nunca, asi que sobrevive al Sort y CRG-72 queda
          intacta.
    RECOMENDACION: (b), y el motivo no es el purismo — es que el NIVEL 2 (#70)
    necesita exactamente lo mismo. Un intent de arrastre tiene que decir «mové
    ESTA celda al slot 12», y eso es un ref que nombra una celda. `sid` se paga
    una vez y cierra los dos frentes.

    VOTOS DEL AUTOR (2026-08-19), tomados ANTES de escribir:
      - (b), el campo estable. En el codigo se llama `cid` y no `sid`: `sid`
        significa SteamID en las 40 apariciones que tiene el modulo, cuatro de
        ellas en GetRecord, que es la funcion vecina de la que lo hospeda.
      - La celda perdida FALLA Y AVISA, no cae a la primera. Cae un clic en una
        carrera rara; lo que se compra es que si algo se mueve, es la celda que
        apretaste.

    LO ESCRITO: `cid` estable por entrada, acuñado una vez en el mismo funnel
    del `ord` (StampEntries -> StampOrd + StampCid, llamado por SaveRecord y
    BuildSnapshot) y nunca reasignado, con el contador EN EL RECORD y persistido
    porque derivarlo de lo vivo reciclaria numeros; FindEntry honra el `cid` y
    sigue cayendo a la primera cuando el ref no lo trae (savegames, import LAN);
    FindCell para los cinco caminos que mueven una celda, con el aviso de la
    celda perdida; el `cid` en EntrySnapshot y en QuickTarget; y Grid.RefOf del
    lado del cliente, que es el unico sitio donde el cliente arma un ref de
    stack. CRG-72 quedo ENMENDADA en su mitad de alcance, explicito en §7.2.

    EL FRENTE QUE EL PEDIDO NO NOMBRABA, y salio del censo: QuickTarget ELEGIA
    una entrada —la regla de la #66, el frasco mas gastado— y despues le
    entregaba a UseEntry un ref que resolvia al PRIMERO. La eleccion se
    calculaba y se tiraba. Es el segundo y ultimo sitio del modulo que arma un
    ref de stack a mano; el censo con denominador (45 lineas con `condition =`,
    19 tablas literales) es lo que lo encontro y lo que evito tocar las otras 17.

    LA LECCION DE METODO: el sabotaje 14 —«el boton Sort pisa el cid»— salio
    VERDE la primera vez, y no por el codigo: en un grid RECIEN NACIDO los `cid`
    coinciden con sus posiciones, asi que pisarlos con la posicion re-escribe los
    MISMOS numeros. El check estaba verde por una coincidencia del sujeto y no
    por el mecanismo. Se arreglo dandole churn al grid del check y una
    PRECONDICION que lo delata.

    Y OJO CON EL ALCANCE: el arreglo es POR CAMINO, no global. Belt, drop, use,
    equip y sub-slot pasan a nombrar la celda; trade y transferencia CONSERVAN
    la semantica de cantidad (el agregado es lo que mantiene alcanzable al stack
    gemelo — informe en juego 2026-07-14).

    LO QUE NO ES UN BUG, para no perseguirlo: que exista un stack de 107 es
    correcto. `AbsorbType` del pool (corpus_cargo_ammopool.lua) devuelve al
    cinturon lo que sale de un cargador hasta el techo y derrama el resto al
    grid via GiveOrDrop -> AddStack, que mergea bajo `max_stack`. Un 107 es una
    descarga que volvio, no municion mal partida.


69. LA GRAMATICA DEL MOUSE: M1 SELECCIONA, M3 DESELECCIONA, M2 ES EL MENU
    [Cargo ui/containers] — CERRADA el 2026-08-20, acunada como CRG-74 con
    sede Cargo_Architecture.md §15.6. PASADA EN JUEGO OK: planilla AE
    (dev/checks/cargo-mouse-r1.html) 14 de 14, 0 fallas, en dos rondas.
    CHANGELOG 72 [APLICADO].
    AE2 CERRO LA UNICA INCOGNITA de la norma: el boton del MEDIO llega a un
    DButton Droppable, cuatro disparos y repetible, asi que lo que era
    indicio sobre la capa de drag-and-drop quedo MEDIDO EN EL MOTOR.
    Los repartos que trajo el autor miden ademas lo que el bloque no se
    propuso: 120+120+17 en la caja y 120+90 en el grid SUMAN 467, o sea que
    la conservacion se sostiene y el 17 es re-empaque bajo el techo.
    OBSERVADO Y ACEPTADO: arrastrar sigue moviendo el stack ENTERO en las
    dos direcciones. Es por diseno (§15.6): el arrastre no es un clic.
    SE DICE QUE SE MIDIO CON GESTO Y QUE POR COBERTURA: AE10/AE12/AE13/AE14
    se acreditan por el uso extendido del trader y por las filas que las
    cubren, no por una corrida aislada (declarado por el autor).
    Harness 1038 (eran 985), 53 checks nuevos y 19 sabotajes en rojo 19 de
    19. glua_check 48/48. Selftest sin moverse (100/107): no toca su
    superficie.

    LOS DOS VOTOS QUE EL AUTOR DIO ANTES DE QUE SE ESCRIBIERA UNA LINEA, y
    los dos quedan como EXCEPCIONES DECLARADAS de la norma:
      · M3 NO HACE NADA EN EL LOOT. La recomendacion del diseno era la
        transferencia inversa (M3 manda de vuelta, mismos tres niveles) y la
        rechazo: «es contraintuitivo, jamas habia visto un sistema asi de
        inventario en juego donde tomes por transferencia inversa». La norma
        se lee entonces «M3 deselecciona DONDE HAY ALGO SELECCIONADO», y el
        camino de vuelta sigue siendo M1 sobre la otra columna.
      · LA FILA DEL BASKET CONSERVA M1 = SACAR, pero gradado. La
        recomendacion era dejarla como «sacar todo» y decir por que; eligio
        darle las cantidades: «es mas consistente de verdad, apretar shift+m1
        se va a aprender facilmente para todo, asi el jugador no memoriza mil
        formas distintas de interactuar con funcionalidades similares». Es el
        unico lugar del modulo donde M1 no agrega, y el motivo es que una
        fila es una entrada de LISTA y no una celda.
    Y confirmo la derivacion que faltaba: M3 es la RUEDA DEL MEDIO.

    LO QUE LA TANDA DEJO COMO METODO, y no es del feature:
      · UNA CASA UNICA SE MIDE POR LA AUSENCIA DEL RESTO. Que la funcion
        exista no dice nada; lo que la hace unica es que NADIE MAS lea una
        tecla. El gate cuenta los CUATRO unicos usos de input.IsKeyDown(KEY_)
        del modulo y prohibe el patron en los otros tres archivos.
      · MUDAR UNA LINEA ANCLADA DESARMA EN SILENCIO UNA VERIFICACION VIEJA.
        La gradacion se fue de corpus_cargo_trade.lua a corpus_cargo_grid.lua
        y dejo el sabotaje 10 de sabotaje_cargo_67.py apuntando a nada: ese
        script NO revienta, imprime ANCLA x0 y sale 1. Se re-apunto y se
        re-corrio (12/12); el del #68 sigue en 16/16.

    LO QUE SIGUE ABAJO ES EL RELEVAMIENTO CON QUE SE ESCRIBIO, y se deja
    porque es lo que explica por que la entrada cambio de titulo.

    LO QUE PIDIO: «m1 en el inventario deberia enviar al otro contenedor o mi
    inventario, deberia funcionar el shift+m1 para enviar todo el item de un
    lado al otro».

    ESTADO REAL HOY (corpus_cargo_ui.lua): estando en Loot, M1 sobre una celda
    YA transfiere —manda `entry.count or 1`, o sea el stack entero de esa
    celda— y el click derecho abre el menu de cantidad (1 / cantidad... / stack
    entero). O sea que la mitad del pedido ya esta y lo que falta es la
    GRADACION, que ademas quedo INCONSISTENTE con el trade despues del #67:
    ahi un clic carga un cuarto del techo y SHIFT el stack. Dos pantallas del
    mismo modulo con dos gramaticas distintas para el mismo gesto.

    VOTADO POR EL AUTOR el 2026-08-19, al correr la planilla AD del #68. Ya no
    hay que votar la convencion: la eligio, y es la del trade, mas una MITAD
    NUEVA que el pedido original no tenia.

    LA NORMA, en sus palabras: «como norma es que M1 selecciona, M3
    deselecciona y M2 es el boton contextual». O sea que esto dejo de ser una
    entrada sobre contenedores y paso a ser la gramatica del mouse del modulo
    ENTERO — y por eso cambio de titulo.

      · M1          -> un cuarto del stack
      · SHIFT+M1    -> el stack clicado, completo
      · ALT+SHIFT+M1-> todos los del mismo tipo
      · M3          -> lo mismo pero DESELECCIONANDO (un cuarto / el stack /
                       todos del tipo). ESTA MITAD NO EXISTE EN NINGUNA
                       PANTALLA HOY: en el trade se saca del basket sin
                       gradacion, y en el loot no hay «deshacer» de ningun tipo.
      · M2          -> el menu contextual, y nada mas. El «enviar cantidad» que
                       ya tiene se queda como esta: el autor lo declaro bien.

    ESTADO REAL MEDIDO (2026-08-19, contra el codigo vivo):
      · LOOT: M1 manda `entry.count or 1` — el stack ENTERO de esa celda, sin
        gradacion. No hay SHIFT ni ALT. (corpus_cargo_ui.lua, onLeftClick de
        los dos grids.)
      · LOOT, el menu de M2: «Take/Move 1», «amount...» y «whole stack». El
        `amount...` anuncia `1 - <count de la celda>` y clampea con
        `math.min(n, entry.count)` — o sea AL CELL, no al agregado
        (corpus_cargo_transfer.lua:55 y :60). El server SI derrama sobre las
        entradas que respaldan el ref (StackTotal/DrainStack), asi que el tope
        es del CLIENTE y es una decision, no una falla. Coherente con «una
        celda es un stack» del #67; incoherente con el agregado del trade. La
        planilla AD11 pidio 200 sobre una celda de 120 y por eso salio rojo: el
        criterio estaba escrito desde la capacidad del server y redactado como
        un gesto de interfaz. Se RETIRO por premisa, no es un defecto abierto.
      · TRADE: M1 = un cuarto del techo, SHIFT+M1 = el stack clicado,
        ALT+SHIFT+M1 = todo (#67). La mitad de M3 no existe.

    LO QUE YA NO DEPENDE DEL #68: «el stack clicado» necesitaba que el ref
    nombrara la celda, y eso CERRO. La mitad de M1 en el loot se puede escribir
    entera; la de M3 es toda nueva.

    OBSERVADO Y ACEPTADO en la misma pasada, para que nadie lo lea como bug
    despues: SHIFT+M1 dos veces sobre el mismo item carga el stack DOS VECES en
    el basket. Es correcto — `BasketAdd` clampea contra `Available - have`
    (corpus_cargo_trade.lua:125), asi que acumula pero nunca pasa del total real
    del lado. El autor lo miro y dijo «igual no lo veo mal».


70. NIVEL 2 DEL GRID: POSICIONES REALES [Cargo ui/inventory] — PEDIDO DEL
    AUTOR 2026-08-19, SIN DISENAR. Es la ultima de las tres sesiones.

    POR QUE: el #67 le saco al orden el ser una funcion del contenido, pero el
    autor lo miro en juego y «igual se ve medio desordenado». La causa que el
    #67 NO toco es el empaque: el DIconLayout es flex-wrap y la altura de una
    fila es la del tile mas alto, asi que un 1x1 al lado de un rifle 2x3 deja
    celdas muertas debajo del chico que no las ocupa nadie. Eso no se arregla
    ordenando: se arregla dandole a cada item una CELDA.

    QUE INVOLUCRA (relevado, no disenado):
      · Un `slot` por entrada (indice fila-mayor sobre un ancho FIJO de
        columnas, alto infinito — el limitante sigue siendo el peso, no el
        espacio).
      · Cambiar el DIconLayout por un canvas de posicion absoluta.
      · Colision de footprints w x h: colocar, rechazar, o empujar.
      · Re-empaque al cargar si el ancho de columnas cambio (resolucion
        distinta, mochila distinta) — un `slot` guardado con otro ancho apunta
        a otro lado.
      · El boton Sort pasa de re-ordenar a EMPAQUETAR.
      · Un ref que nombre la celda para el intent de arrastre: ES EL MISMO
        `sid` del #68. Hacer el #68 primero no es orden arbitrario, es que
        paga la mitad de este.

    Y HAY QUE DECIRLO EXPLICITO: esto ENMIENDA la decision del 2026-07-11 («el
    footprint es solo render, sin gestion espacial, el costo de cargar es peso
    y no espacio»). La primera mitad se cae —va a haber gestion espacial—; la
    segunda NO: el grid sigue siendo infinito hacia abajo y el techo sigue
    siendo el peso. Que quede escrito cual de las dos mitades se enmienda es
    justo lo que evita que dentro de un mes alguien lea la enmienda como
    permiso para poner un limite de casillas.

71. QUE NUMERO MUESTRA EL CHIP DE UN ITEM CON USOS [Cargo ui] — PEDIDO DEL AUTOR
    el 2026-08-19, ABIERTA, SIN DISENAR.
    DE DONDE SALE: de la nota del check 07 de la pasada de la #66, textual — «el
    wheelmenu y el quickmenu deberian mostrar del item la cantidad de usos mas
    que la cantidad del item, o nose, tal vez deberia mostrar la cantidad de uso
    de ese item».
    POR QUE NO ES UN CAPRICHO, y esto es lo que la vuelve una entrada: el chip
    dice «x2» —dos frascos— pero la tecla dispara sobre UNO SOLO, el mas gastado
    (la regla que se voto en la #66). O sea que EL NUMERO QUE SE MUESTRA NO
    PREDICE LO QUE VA A PASAR AL APRETAR: con dos frascos, uno en 0/3 y otro en
    3/3, el chip dice «x2» y lo que va a salir es una pastilla del segundo. Es la
    MISMA pregunta de la #66 —cual es la unidad— sobre una cuarta superficie que
    esa tanda no censo, porque el censo se hizo sobre las que dibujan CONDICION y
    el chip dibuja CUENTA.
    IRONIA UTIL PARA NO REPETIRLA: el parche 6 de la #66 toco justo ese
    QuickCount — lo arreglo para que contara las DOS clases, porque con uniques
    pintaba un «x0» en rojo sobre una mochila con dos frascos. Quedo contando
    bien LA COSA EQUIVOCADA. Arreglar el conteo de algo no responde si ese algo
    era lo que habia que contar.
    LAS TRES FORMAS, y el autor no eligio (dijo «o nose, tal vez»), asi que ESTO
    SE VOTA ANTES DE ESCRIBIR:
      a) los usos del que la tecla VA A USAR («2» = las que quedan en el frasco
         que sale). Es el unico que predice el proximo apretar, y es la unica
         forma que NECESITA resolver la instancia del lado del cliente — hoy esa
         regla vive SOLO en el server (QuickTarget), asi que habria dos copias de
         un criterio, que es exactamente lo que el ecosistema evita. Medir eso
         PRIMERO: si duplica la regla, la (a) es mas cara de lo que parece.
      b) la suma de usos de todos («5/6»). No duplica la regla y no predice nada.
      c) las dos cosas («x2 · 2»). Es la salida del tooltip de la #66, y en 56 px
         de chip puede no entrar: medir el ancho antes de votarla.
    LO QUE NO HAY QUE HACER: tocar la regla de seleccion. Esta votada, medida y
    verde en juego — esto es que numero se DIBUJA, no cual se USA.
    ALCANCE: el chip del wheel (§17) y la celda quick del frame (§15). Son dos
    superficies y hoy leen la MISMA funcion (CARGO.UI.QuickCount), asi que si se
    cambia, se cambia ahi y no en los dos lugares — mismo argumento que llevo el
    texto de condicion a Theme.ConditionShort en la #66.

72. DROP DESDE EL CINTURON [Cargo ui/inventory] — CERRADA el 2026-08-20, en
    tanda con la #74. PASADA EN JUEGO OK: planilla AG
    (dev/checks/cargo-belt-drop-r1.html). CHANGELOG 74 [APLICADO].
    CUATRO filas cerraron POR GESTO y el autor las nombro una por una: que
    bota de a 1 y el stack entero (dos cantidades distintas), que Return to
    inventory sigue andando despues de botar, y —la estrella— que RECARGAR
    despues de botar toma de la reserva correcta. Esa ultima es la unica
    fila del bloque que NO se mira en pantalla: es la que convierte el
    AmmoPool.Push de una linea escrita en una linea medida. Las otras seis
    se acreditan por cobertura («no he visto ningun problema») y se declara
    la diferencia.
    QUE SE ESCRIBIO: CARGO.Inventory.BeltDrop(ply, slotN, count) + intent
    belt_drop, con la forma de la rama de stack de DropEquipped (#28) y no
    una ruta nueva; y el menu de M2 del slot pasa de UNA opcion a TRES
    —Return to inventory, Drop, Drop all (xN)— con el vocabulario del GRID
    palabra por palabra (voto del autor). El ARRASTRE no bota (voto del
    autor): el drag del cinturon ya significa devolver al grid o reordenar.
    NO ACUNA CRG: es aplicacion de CRG-15 / §16.3. Lo que si se hizo es
    escribir en §16.3 la TABLA DE LAS CUATRO PUERTAS que empujan el espejo
    (BeltSet, BeltMove, BeltClear, BeltDrop), justo porque la que falte NO
    DA ERROR.
    LA LECCION DE LA TANDA, y la dijo el sabotaje y no la lectura: el
    control del gate de RANGO de slot, escrito sobre slots VACIOS, pasaba
    con el gate puesto Y con el gate sacado —un slot fuera de rango siempre
    esta vacio, asi que el `entry == nil` contestaba primero y el guardia
    nunca se ejercia. Un guardia cuya unica prueba no puede alcanzarlo es un
    guardia que nadie midio. Se reescribio plantando una entrada en belt[9].
    Verificacion: 28 checks nuevos (20 server + 8 cliente) y 9 sabotajes en
    rojo, entre ellos el que le saca el AmmoPool.Push al drop nuevo — si ese
    saliera verde, el check estrella no mediria.

72. (texto original de la entrada, para leer de que salio)

    LO QUE PIDIO, textual: «Falta drop desde el belt para expulsarlo del
    inventario a la municion».

    ESTADO REAL: la munición que está en un slot del cinturón sólo puede
    volver al grid (BeltClear) y recién desde ahí botarse. O sea que sacar 120
    balas del inventario son DOS gestos, y el segundo depende de que haya lugar
    de peso en el grid — que es justo lo que no hay cuando querés tirar algo.
    Los otros dos contenedores de equipo ya tienen su salida directa:
    `DropEquipped` (roadmap #28) bota desde un slot de equipo sin pasar por el
    grid, y esa es la maquinaria a la que esto se parece.

    LO QUE HAY QUE MIRAR ANTES DE ESCRIBIR: el cinturón NO es almacenamiento
    inerte, ES el pool de munición del engine (§16, roadmap #19). Un drop tiene
    que pasar por el mismo espejo que BeltClear —AmmoPool.Push— o el jugador
    tira las balas y las sigue teniendo cargadas. Ese es el defecto que la
    entrada existe para evitar, y no se ve en pantalla: se ve al disparar.

73. SEPARAR UN STACK A MANO [Cargo ui] — PEDIDO DEL AUTOR el 2026-08-19,
    al correr la planilla AD del #68. SIN DISENAR.

    LO QUE PIDIO: «creo que si hace falta una funcionalidad para separar
    stacks, tal vez dentro del menu contextual».

    DE DONDE SALE, y esto es lo que la vuelve una entrada en vez de un
    capricho: la planilla misma lo necesitó. Las filas AD7 y AD8 —los caminos
    `UseEntry` y `SubSlotAttach`— NO se podían correr con el kit de dev, porque
    `cargo_dev_medkit` y `cargo_dev_plate` no declaran `max_stack` y entonces
    el grid nunca los parte en dos celdas. Hubo que partirlas con un `lua_run`
    sobre el record. Si el instrumento necesita una función que el juego no
    tiene, el jugador la necesita también.

    LO QUE HABILITA, que es más que la comodidad: hoy la única forma de tener
    dos celdas del mismo ítem es que `max_stack` las parta. Con separar a mano,
    el jugador puede dejar 30 balas en el cinturón y el resto en la mochila sin
    depender del techo — y eso ya es una decisión de carga, que es de lo que se
    trata el módulo.

    OJO CON EL ALCANCE: separar es lo inverso de `AddStack`, y `AddStack`
    MERGEA solo. Una celda separada que se toca con la de al lado se vuelve a
    fundir en el próximo funnel si la regla no dice otra cosa — o sea que esto
    NO es sólo un botón: es decidir si el merge automático deja de ser
    automático, y eso toca el #67 y el #70. Medir eso PRIMERO.

75. EL CUADRO DE «HOW MUCH» ESTA SIN TEMATIZAR [Cargo ui] — CERRADA el
    2026-08-20, el mismo dia que se pidio. Amplia CRG-75 a los prompts.
    PASADA EN JUEGO OK: planilla AH (dev/checks/cargo-prompt-tema-r1.html).
    CHANGELOG 75 [APLICADO].
    Las filas de COLOR se acreditan por declaracion directa del autor («los
    colores del menu contextual y de los StringRequest estan perfectos»); las
    dos de CONDUCTA —que el campo muestre lo que se tipea y que aceptar
    transfiera— por USO EXTENDIDO («varias horas» de juego), y se dice cual es
    cual. Para la fila del texto ese uso es MEJOR evidencia que un gesto
    unico: si DrawTextEntryText faltara, el numero llegaria igual al server y
    lo unico visible seria la caja vacia al escribir.

    LO QUE PIDIO, textual: «el unico cambio menor que quiero es que el menu
    de "how much" cuando mandas por amount o compras por amount tambien tiene
    que tener el color del hud asi como el menu contextual».

    ES LA MISMA NORMA CON OTRA PUERTA, y por eso NO acuna un CRG nuevo:
    AMPLIA CRG-75, que nacio nombrando solo DermaMenu(). Dos IDs para «la UI
    del modulo pasa por el theme» seria justo la duplicacion que la norma
    existe para evitar.
    DE DONDE SALIO, y vale anotarlo como forma: el cuadro era la ULTIMA
    superficie Derma de fabrica del modulo, y solo se hizo visible cuando los
    menus dejaron de ser grises. Arreglar lo que se veia dejo a la vista lo
    que quedaba debajo.

    QUE SE ESCRIBIO: CARGO.Theme.Prompt(), y los DOS sitios lo llaman (el
    Buy/Sell amount... del trade y el Take/Move amount... del loot). Envuelve
    Derma_StringRequest en vez de re-implementarlo: la funcion del engine
    posee el LAYOUT —dimensiona desde su etiqueta, centra, cablea el Enter,
    hace la ventana modal— y reescribir eso para cambiar seis colores seria
    un diff mucho mayor con un modo de falla mucho peor.

    ⭐ EL RENGLON QUE NO SE VE COMO UN COLOR EQUIVOCADO SINO COMO «NO HAY
    TEXTO»: el texto de un DTextEntry NO lo dibuja el engine solo, lo dibuja
    el SKIN (skins/default.lua PaintTextEntry termina en
    panel:DrawTextEntryText). Un Paint propio que reemplace al skin y se
    olvide de esa llamada deja UNA CAJA DONDE SE PUEDE TIPEAR Y NO SE VE
    NADA. Leido en la fuente antes de escribir, no descubierto en juego.
    Tiene check de conducta Y sabotaje propio.

    Verificacion: 18 checks nuevos (11 de conducta en cliente + 7 en el gate
    de FUENTES, dos POR CUENTA y por archivo separado) y 8 sabotajes en rojo;
    la tanda entera queda en 28/28.

74. EL MENU CONTEXTUAL ESTA SIN TEMATIZAR [Cargo ui] — CERRADA el 2026-08-20,
    acunada como CRG-75 con sede Cargo_Architecture.md §15.7. PASADA EN
    JUEGO OK: planilla AF (dev/checks/cargo-menu-tema-r1.html), declarada
    como BARRIDO y no fila por fila («ya revise todas y funciona como
    corresponde») — se dice porque un verde por cobertura y uno por gesto no
    son lo mismo. Lo que la declaracion SI cubre sin ambiguedad es lo unico
    que el harness no podia cerrar solo: que ningun menu quedara afuera de
    la norma. CHANGELOG 73 [APLICADO].
    QUE SE ESCRIBIO: CARGO.Theme.Menu(), la PUERTA UNICA, y los OCHO sitios
    que abrian un DermaMenu() pelado pasan a llamarla (6 en ui.lua, 1 en
    trade.lua, 1 en transfer.lua). El tenido de DGL4 sale gratis: T.Colors
    ya deriva del acento del mod y muta EN SITIO (CRG-29), asi que no hay
    una sola linea de compat nueva. Votos del autor: los OCHO menus, y hover
    cellHover + borde border (sin acento pleno, que en una lista de ocho
    opciones queda chillon).
    ⚠ LA PREMISA CON LA QUE SE ENTRO A LA TANDA ERA FALSA, y se corrigio
    leyendo la FUENTE de GMod en disco (vgui/dmenu.lua + dmenuoption.lua):
    NO es cierto que «un DMenu hijo se crea al abrirse». Se crea al
    DECLARARSE — DMenuOption:AddSubMenu() lo construye en el acto con un
    DermaMenu(true, self) PELADO. La conclusion (pintar la DESCENDENCIA y no
    la instancia) sobrevivio intacta, pero por otro mecanismo: con la
    premisa falsa el helper se habria escrito enganchando el Open() del
    hijo, mas caro y apuntado al lugar equivocado.
    Verificacion: 22 checks nuevos (12 de conducta en cliente + 10 en el
    gate de FUENTES, tres de ellos POR CUENTA y por ARCHIVO SEPARADO, porque
    con el total mudar un menu de archivo lo tapa) y 11 sabotajes en rojo.
    NO se toco el CONTENIDO de ningun menu: es pintura.

74. (texto original de la entrada, para leer de que salio)

    LO QUE DIJO: «tambien el menu contextual tiene color derma, deberia tomar
    el color del hud de DGL4 que tiene cargo».

    ES EL UNICO PEDAZO DE UI DEL MODULO QUE NO PASA POR EL THEME. Todo lo demás
    —el frame, las celdas, el wheel, el tooltip, los chips— deriva su paleta de
    `CARGO.Theme`, que a su vez la saca del acento de HoloHUD/DGL4 cuando está
    montado (§theme). El menú del click derecho es un `DermaMenu()` pelado
    (corpus_cargo_transfer.lua y corpus_cargo_ui.lua), así que aparece con el
    gris de fábrica de Derma encima de una interfaz que no es gris.

    ES CHICO PERO NO ES TRIVIAL: `DermaMenu` no acepta una paleta, hay que
    pintarle el `Paint` al panel y a cada `DMenuOption`, y los sub-menús se
    crean solos al abrirse. La forma correcta es un helper ÚNICO
    —`Theme.Menu()`— y que los tres o cuatro sitios que abren menús lo llamen,
    en vez de repetir el pintado; mismo argumento que llevó el texto de
    condición a `Theme.ConditionShort` en la #66 y el criterio de orden a
    `Items.AutoSortLess` en el #67.

76. EL GRID DEL JUGADOR DENTRO DEL LOOT NO TIENE LAS ACCIONES BÁSICAS
    [Cargo ui] — PEDIDO DEL AUTOR el 2026-08-21, SIN VOTAR, SIN ESCRIBIR.
    NACIÓ DE LA NOTA DE AI5, un check que PASÓ (octava vez en el arco).
    QUÉ DIJO, textual: «falta como QoL, que puedas quitarle o ponerle favorito
    en la ventana de loot, porque lo actual hace que el jugador deba salir,
    entrar al inventario y ahí quitarle el favorito (el arma que está equipada
    ya puede hacer eso pero no el grid del inventario). También, falta que se
    pueda dropear un ítem de tu grid mientras estás en la pantalla del loot,
    porque hasta ahora con el botón contextual sólo puedes mover; por eso
    faltan las funcionalidades básicas que existen en el grid del inventario
    normal».
    LOS DOS HUECOS SON UNO SOLO Y ESTÁ VERIFICADO EN EL ÁRBOL: el grid del
    jugador dentro del loot abre `CARGO.Transfer.Menu`
    (client/corpus_cargo_transfer.lua), que construye SÓLO los verbos de
    transferencia —Move 1 / Move amount… / Move whole stack—, mientras que el
    menú del grid del inventario (client/corpus_cargo_ui.lua) trae «Mark as
    favorite» Y «Drop»/«Drop all», y el del slot de equipo también. Eso último
    es exactamente por qué el autor observó que «el arma que está equipada ya
    puede hacer eso»: el slot de equipo usa el menú completo y el grid del
    loot no.
    NO ES UN DEFECTO DE LA #43: el menú del loot nació con menos filas mucho
    antes de que los favoritos existieran, y la #43 no lo tocó. Lo que la #43
    hizo fue volverlo VISIBLE — antes nadie necesitaba desmarcar nada desde
    ahí, y ahora un favorito obliga a cerrar el loot, abrir el inventario,
    desmarcar, y volver.
    LO QUE HAY QUE DECIDIR ANTES DE ESCRIBIR, y es lo que la vuelve una
    entrada y no un parche de tres líneas: si los dos menús se UNIFICAN (un
    solo constructor que reciba qué verbos ofrecer, con el de transferencia
    como el extra del contexto de loot) o si el del loot simplemente CRECE dos
    filas. La primera es el mismo argumento del helper único que llevó a
    `Theme.Menu()` en el #74 y a `Theme.ConditionShort` en el #66; la segunda
    es más barata hoy y deja dos casas que se van a desincronizar la próxima
    vez que el menú del inventario gane una fila — que es literalmente lo que
    acaba de pasar.
    LA PREGUNTA DE ALCANCE YA ESTA CONTESTADA, y el autor la cerró como una
    REGLA DE VOCABULARIO y no como un caso: «"drop" es siempre tirar al piso,
    al contenedor es "mover"» (2026-08-21). O sea que el rótulo no depende de
    la ventana: dentro del loot «Drop» bota al PISO igual que en el inventario,
    y mandar al contenedor sigue siendo «Move», que es lo que ese menú ya hace.
    Los dos verbos conviven en la misma pantalla sin ambigüedad porque nombran
    DESTINOS distintos y no la misma acción en dos contextos.
    ⚠ LO QUE ESA REGLA DEJA COMO CONSECUENCIA, y hay que mirarlo al escribir:
    el menú del loot va a ofrecer «Move» y «Drop» uno al lado del otro sobre la
    misma celda, así que el orden y la separación importan más que de
    costumbre — es el único menú del módulo donde un clic de más manda el ítem
    a un lugar del que hay que ir a buscarlo. El menú del grid del inventario
    no tiene ese problema porque ahí «Move» no existe.

77. LA FILA DE TABS SE PARTE EN DOS A 1280x720 Y 1366x768 [Cargo ui] — MEDIDO
    el 2026-08-21, SIN ESCRIBIR. PRIORIDAD BAJA, y el motivo es que el autor
    NO LA VE: juega a 1920x1080 nativo (borderless, aspecto Any/All, o sea
    16:9), donde la fila entra con 190 px DE SOBRA. Es un defecto de las
    resoluciones chicas, y el destino es Workshop.
    QUE ESTA MEDIDO, y ahora con instrumento guardado: dev/medir_fila_de_tabs.py.
    Las ocho tabs suman 454 px —412 de botones (bw = tw + 22) más SIETE gaps de
    6— contra el ancho que la barra recibe, que es `colR - 16` y escala con
    ScrH: 424 a 720p, 453 a 768p, 644 a 1080p. Así que `Misc` cae a la segunda
    fila a 1280x720 (por 30 px) y a 1366x768 (POR UN PIXEL).
    LO QUE LO CAUSA NO ES EL LARGO DE UN NOMBRE SINO QUE LAS FUENTES NO ESCALAN:
    T.UIScale deja las fuentes fijas a propósito —las de VGUI no se pueden
    redimensionar por frame y está escrito— así que las tabs miden LO MISMO en
    toda resolución y lo único que se mueve es la barra. Por eso acortar «Misc»
    o «Weapons» no arregla nada en ninguna resolución: lo que decide es la
    CANTIDAD de tabs. Esto ya había decidido la UI del #43 — el toggle ★ se
    eligió sobre una novena tab justamente por esto.
    EL COMENTARIO DEL CODIGO AFIRMA LO CONTRARIO Y ES FALSO:
    shared/corpus_cargo_items.lua dice que el set fijo entra en una fila «at
    every aspect we ship». Aunque no se arregle el layout, ESA LINEA hay que
    corregirla — un comentario que promete lo que el código no cumple es peor
    que ninguno, porque el próximo que agregue una tab lo va a creer.
    ⚠ EL CASO DE 1366x768 NO SE CIERRA OFFLINE: da por UN pixel, y PIL rasteriza
    con su motor y GMod con el de Windows. A 1280x720 el margen es de 30 px y
    ahí la conclusión aguanta. Si alguna vez se toca, la fila de 768 se mira en
    juego o no se afirma.
    LA SALIDA NO ES OBVIA Y NO ESTA VOTADA: el wrap YA existe como red de
    seguridad y la barra crece (`bar:SetTall(rowY + 26)`), así que la segunda
    fila SE VE — no hay nada recortado ni perdido. O sea que esto es de
    prolijidad, no de función, y perfectamente puede cerrarse decidiendo que
    dos filas a 720p está bien y arreglando sólo el comentario.
